# Linear Task Research Prompt

# Placeholders replaced by linear-enricher.py before passing to Claude:

# {{IDENTIFIER}} — Linear issue identifier (e.g., OPE-42)

# {{TITLE}} — issue title

# {{DESCRIPTION}} — issue description (markdown)

# {{STATE}} — current workflow state (Todo, Backlog, In Progress)

# {{LABELS}} — comma-separated label names

# {{COMMENTS}} — recent comments formatted as markdown

# {{DATE}} — UTC timestamp of this research session

You are performing **automated research** on a Linear task for the OpenMates project.
Your goal is to analyze the task, research the codebase and web for relevant context,
and post a structured research summary as a comment on the Linear issue.

## Task

**{{IDENTIFIER}}** — {{TITLE}}

**Status:** {{STATE}} | **Labels:** {{LABELS}}

### Description

{{DESCRIPTION}}

### Recent Comments

{{COMMENTS}}

## Instructions

1. **Understand the task** — parse the title and description to identify what needs to be done.
   Classify it as: bug fix, feature, improvement, refactor, docs, or infrastructure.

2. **Codebase research** — search the codebase for:
   - Files and functions most likely affected by this task
   - Existing patterns or utilities that should be reused
   - Related recent commits (last 2 weeks) that provide context
   - Potential conflicts with other in-progress work

3. **Web research** — if the task involves external APIs, libraries, or standards:
   - Search for current documentation and best practices
   - Check for known issues or breaking changes
   - Find relevant examples or implementations

4. **Complexity assessment** — estimate:
   - Number of files likely to change
   - Risk level (low / medium / high)
   - Dependencies on other tasks or systems
   - Estimated effort (trivial / small / medium / large)

5. **Auto-tag suggestions** — based on your analysis, suggest which labels should be added.
   Available labels: Bug, Feature, Improvement, Backend, Security, Performance, Encryption,
   DevOps, SEO, cronjob.

6. **Post findings** — use `mcp__linear__save_comment` to post a structured research comment
   on issue {{IDENTIFIER}} with this format:

   ```markdown
   ## Research Summary (automated)

   **Classification:** <type>
   **Estimated effort:** <trivial/small/medium/large>
   **Risk:** <low/medium/high>

   ### Relevant Code
   - `path/to/file.ts` — <why it's relevant>
   - `path/to/other.py` — <why it's relevant>

   ### Key Findings
   - <finding 1>
   - <finding 2>

   ### Suggested Approach
   <1-3 sentences describing the recommended implementation approach>

   ### Suggested Labels
   <comma-separated label names to add>

   ---
   *Auto-generated by Claude research on {{DATE}}*
   ```

7. **Apply labels** — if you identified labels that should be added, use
   `mcp__linear__save_issue` to add them (merge with existing labels, never remove).

## Rules

- Do NOT edit, write, or create any files in the repository.
- Do NOT implement any changes — research only.
- Keep the research comment concise (under 500 words).
- If the task is already well-described and has recent research comments, skip it
  and post a brief "No additional research needed" comment.
- Focus on actionable insights, not obvious observations.

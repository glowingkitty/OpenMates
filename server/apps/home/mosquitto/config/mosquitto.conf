# Allow anonymous connections (not recommended for production)
allow_anonymous true

# Default listener on port 1883
listener 1883

# Optional: WebSocket listener on port 9001
listener 9001
protocol websockets

# Persistence settings
persistence true
persistence_location /mosquitto/data/

# Log settings
# Change log destination to stdout to see logs in Docker
log_dest stdout
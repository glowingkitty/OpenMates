// import { error } from '@sveltejs/kit';
// import { isPageVisible } from '$lib/config/pages';

// export function load({ url }) {
//     if (!isPageVisible(url.pathname)) {
//         throw error(404, 'Page not found');
//     }
    
//     return {};
// } 
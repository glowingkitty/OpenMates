---
description: Load the create-pr skill and create a dev-to-main pull request after changelog review and five clarifying questions
---

Load the `create-pr` skill with the `skill` tool and follow it completely. This
is an explicit request to prepare and create the `dev` to `main` pull request
after the skill summarizes changelog Markdown and asks exactly five clarifying
questions, one per assistant message.

Optional PR title guidance from the user: $ARGUMENTS

---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/packages/ui/src/tokens/generated/swift/ColorTokens.generated.swift
  - frontend/packages/ui/src/tokens/generated/swift/SpacingTokens.generated.swift
  - frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift
  - frontend/packages/ui/src/tokens/generated/swift/GradientTokens.generated.swift
  - apple/OpenMates/Sources/Shared/Extensions/DataExtensions.swift
  - apple/OpenMates/Sources/Shared/Extensions/FontRegistration.swift
autonomous: true
requirements: [FOUN-01, FOUN-03]

must_haves:
  truths:
    - "Every hex value in ColorTokens.generated.swift matches the corresponding value in colors.yml and theme.generated.css"
    - "Every spacing/radius value in SpacingTokens.generated.swift matches spacing.yml and radii.yml"
    - "Every font size in TypographyTokens.generated.swift matches typography.yml pt values"
    - "Every gradient hex pair in GradientTokens.generated.swift matches gradients.yml"
    - "Gradients for 'openmates' and 'chat' app categories exist in Swift and use primary colors #4867cd/#5a85eb"
    - "DataExtensions.swift openMatesOfficial gradient uses the same hex values as the generated primary (#4867cd/#5a85eb), not stale values"
    - "Lexend Deca font is registered for all 6 weights in FontRegistration.swift and Info.plist"
    - "lint-swift-design-tokens.sh passes with zero violations"
  artifacts:
    - path: "frontend/packages/ui/src/tokens/generated/swift/ColorTokens.generated.swift"
      provides: "Color token extensions matching CSS source"
    - path: "frontend/packages/ui/src/tokens/generated/swift/SpacingTokens.generated.swift"
      provides: "Spacing and radius token extensions matching CSS source"
    - path: "frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift"
      provides: "Typography token extensions matching CSS source with Lexend Deca"
    - path: "frontend/packages/ui/src/tokens/generated/swift/GradientTokens.generated.swift"
      provides: "Gradient token extensions matching CSS source including openmates/chat categories"
    - path: "apple/OpenMates/Sources/Shared/Extensions/DataExtensions.swift"
      provides: "Hand-maintained gradient extensions consistent with generated tokens"
    - path: "apple/OpenMates/Sources/Shared/Extensions/FontRegistration.swift"
      provides: "Font registration for all 6 Lexend Deca weights"
  key_links:
    - from: "frontend/packages/ui/src/tokens/sources/colors.yml"
      to: "frontend/packages/ui/src/tokens/generated/swift/ColorTokens.generated.swift"
      via: "build-tokens.js generation"
      pattern: "Color\\(hex: 0x|Color\\(\""
    - from: "frontend/packages/ui/src/tokens/sources/gradients.yml"
      to: "frontend/packages/ui/src/tokens/generated/swift/GradientTokens.generated.swift"
      via: "build-tokens.js generation"
      pattern: "omGradient\\(start: Color\\(hex:"
    - from: "apple/OpenMates/Sources/Shared/Extensions/FontRegistration.swift"
      to: "frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift"
      via: "Font.custom(\"Lexend Deca\") calls"
      pattern: "Lexend Deca"
---

<objective>
Verify that all generated Swift token files are in sync with their CSS/YML source files, fix any drift found, and confirm Lexend Deca font registration is complete.

Purpose: Tokens are the synchronization mechanism between web and Apple apps. If any generated Swift value differs from the CSS source, every downstream screen inherits the wrong visual. Font registration must be verified because silent fallback to SF Pro causes systemic visual drift invisible in quick testing.

Output: All four generated token files confirmed matching CSS source values. DataExtensions.swift hand-maintained gradients consistent with generated tokens. Font registration verified for all 6 Lexend Deca weights.
</objective>

<execution_context>
@/Users/kitty/git/OpenMates/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kitty/git/OpenMates/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Token pipeline: YML sources -> build-tokens.js -> generated Swift files -->
<!-- The executor must compare generated Swift values against YML source values -->

From frontend/packages/ui/src/tokens/sources/colors.yml:
- Grey scale: 0="#ffffff"(light)/"#171717"(dark), 10="#f9f9f9"/"#1c1c1c", ... 100="#000000"/"#ffffff"
- Grey blue: light="#e6eaff", dark="#2d2f35"
- Font primary: light="#000000", dark="#e6e6e6"
- Font button: "#ffffff" (theme-independent)
- Font field-placeholder: "#9e9e9e"
- Button primary: "#ff553b", hover="#ff6b54", pressed="#ff4422"
- Button secondary: "#808080", hover="#909090", pressed="#606060"

From frontend/packages/ui/src/tokens/sources/spacing.yml:
- spacing-0=0, 1=2, 2=4, 3=6, 4=8, 5=10, 6=12, 8=16, 10=20, 12=24, 16=32, 20=40, 24=48, 32=64

From frontend/packages/ui/src/tokens/sources/radii.yml:
- radius-1=4, 2=6, 3=8, 4=10, 5=12, 6=14, 7=16, 8=20, full=9999

From frontend/packages/ui/src/tokens/sources/typography.yml:
- h1=60pt, h2=30pt, h3=20pt, h4=16pt, p=16pt, small=14pt, xs=13pt, xxs=12pt, tiny=11pt, micro=9pt
- lg=17pt, xl=22pt, xxl=28pt, xxxl=32pt, hero=48pt
- Font family: "Lexend Deca Variable" (CSS) / "Lexend Deca" (Swift custom font name)

From frontend/packages/ui/src/tokens/sources/gradients.yml:
- primary: start="#4867cd", end="#5a85eb"
- openmates: references primary (start=#4867cd, end=#5a85eb)
- chat: references primary (start=#4867cd, end=#5a85eb)
- 48 app gradients with specific hex pairs

Known drift found during planning:
- GradientTokens.generated.swift is MISSING appOpenmates and appChat entries
- DataExtensions.swift has openMatesOfficial with WRONG hex (#6366F1/#4F46E5) vs correct primary (#4867cd/#5a85eb)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and regenerate Swift token files against CSS source</name>
  <files>
    frontend/packages/ui/src/tokens/generated/swift/ColorTokens.generated.swift,
    frontend/packages/ui/src/tokens/generated/swift/SpacingTokens.generated.swift,
    frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift,
    frontend/packages/ui/src/tokens/generated/swift/GradientTokens.generated.swift
  </files>
  <read_first>
    frontend/packages/ui/src/tokens/sources/colors.yml,
    frontend/packages/ui/src/tokens/sources/spacing.yml,
    frontend/packages/ui/src/tokens/sources/radii.yml,
    frontend/packages/ui/src/tokens/sources/typography.yml,
    frontend/packages/ui/src/tokens/sources/gradients.yml,
    frontend/packages/ui/src/tokens/generated/swift/ColorTokens.generated.swift,
    frontend/packages/ui/src/tokens/generated/swift/SpacingTokens.generated.swift,
    frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift,
    frontend/packages/ui/src/tokens/generated/swift/GradientTokens.generated.swift,
    frontend/packages/ui/scripts/build-tokens.js
  </read_first>
  <action>
    Step 1: Run the token build script to regenerate all Swift token files from the YML sources:
    ```
    cd frontend/packages/ui && npm run build:tokens
    ```

    Step 2: After regeneration, diff each generated Swift file against its previous version to identify any changes:
    ```
    git diff frontend/packages/ui/src/tokens/generated/swift/
    ```

    Step 3: Verify the regenerated files by performing a value-by-value spot check of critical tokens:

    ColorTokens.generated.swift must contain:
    - `static let grey0 = Color("grey-0")` (adaptive, uses xcassets)
    - `static let greyBlue = Color("grey-blue")` (adaptive)
    - `static let fontButton = Color(hex: 0xFFFFFF)` (theme-independent)
    - `static let fontFieldPlaceholder = Color(hex: 0x9E9E9E)` (theme-independent)
    - `static let buttonPrimary = Color(hex: 0xFF553B)` (theme-independent)
    - `static let buttonPrimaryHover = Color(hex: 0xFF6B54)`
    - `static let buttonPrimaryPressed = Color(hex: 0xFF4422)`

    SpacingTokens.generated.swift must contain:
    - `static let spacing4: CGFloat = 8` (most common spacing)
    - `static let spacing8: CGFloat = 16`
    - `static let radius3: CGFloat = 8` (most common radius)
    - `static let radius8: CGFloat = 20` (primary buttons)
    - `static let radiusFull: CGFloat = 9999` (pill shapes)
    - `static let breakpointMobile: CGFloat = 730`

    TypographyTokens.generated.swift must contain:
    - `Font.custom("Lexend Deca", size: 60)` for omH1
    - `Font.custom("Lexend Deca", size: 16)` for omP
    - `Font.custom("Lexend Deca", size: 14)` for omSmall
    - `Font.custom("Lexend Deca", size: 13)` for omXs
    - `Font.custom("Lexend Deca", size: 9)` for omMicro

    GradientTokens.generated.swift must contain:
    - `static let primary = omGradient(start: Color(hex: 0x4867CD), end: Color(hex: 0x5A85EB))`
    - `static let appAi = omGradient(start: Color(hex: 0xB85A3A), end: Color(hex: 0xE8956E))`
    - All 48 app gradients from gradients.yml

    Step 4: Check specifically whether the build-tokens.js script generates entries for `appOpenmates` and `appChat` gradients (which reference the primary colors). If not generated, check if the script needs to be updated to handle `start_ref`/`end_ref` entries from gradients.yml. If the script does not produce these, they must be added to DataExtensions.swift in Task 2 instead.

    Step 5: If any diff was produced, commit the regenerated files with message: "chore: regenerate Swift tokens from current CSS source"
  </action>
  <verify>
    <automated>cd /Users/kitty/git/OpenMates && diff <(grep "static let" frontend/packages/ui/src/tokens/generated/swift/SpacingTokens.generated.swift | wc -l) <(echo "23") && echo "spacing count matches" && grep -c "0xFF553B" frontend/packages/ui/src/tokens/generated/swift/ColorTokens.generated.swift | grep -q "1" && echo "buttonPrimary hex correct" && grep -c "size: 16" frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift | grep -q "[2-9]" && echo "font size 16 present for p/h4" && grep -c "0x4867CD\|0x4867cd" frontend/packages/ui/src/tokens/generated/swift/GradientTokens.generated.swift | grep -q "[1-9]" && echo "primary gradient hex correct"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "static let" frontend/packages/ui/src/tokens/generated/swift/ColorTokens.generated.swift` returns a number >= 18
    - `grep "0xFF553B" frontend/packages/ui/src/tokens/generated/swift/ColorTokens.generated.swift` matches exactly the buttonPrimary line
    - `grep "spacing4.*= 8" frontend/packages/ui/src/tokens/generated/swift/SpacingTokens.generated.swift` returns 1 match
    - `grep "radius8.*= 20" frontend/packages/ui/src/tokens/generated/swift/SpacingTokens.generated.swift` returns 1 match
    - `grep "radiusFull.*= 9999" frontend/packages/ui/src/tokens/generated/swift/SpacingTokens.generated.swift` returns 1 match
    - `grep 'size: 60' frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift` matches omH1
    - `grep 'size: 16' frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift` matches omP and omH4
    - `grep 'Lexend Deca' frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift` returns matches for every font size
    - `grep "0x4867CD\|0x4867cd" frontend/packages/ui/src/tokens/generated/swift/GradientTokens.generated.swift` returns at least 1 match for primary gradient
    - `git diff frontend/packages/ui/src/tokens/generated/swift/` shows no uncommitted changes (either no drift existed, or drift was fixed and committed)
  </acceptance_criteria>
  <done>All four generated Swift token files have been regenerated from current CSS sources. Every hex, spacing, radius, font size, and gradient value in the generated files matches the YML source definitions. Any drift that existed has been fixed by regeneration and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix DataExtensions.swift gradient drift and verify font registration</name>
  <files>
    apple/OpenMates/Sources/Shared/Extensions/DataExtensions.swift,
    apple/OpenMates/Sources/Shared/Extensions/FontRegistration.swift
  </files>
  <read_first>
    apple/OpenMates/Sources/Shared/Extensions/DataExtensions.swift,
    apple/OpenMates/Sources/Shared/Extensions/FontRegistration.swift,
    frontend/packages/ui/src/tokens/generated/swift/GradientTokens.generated.swift,
    frontend/packages/ui/src/tokens/sources/gradients.yml
  </read_first>
  <action>
    PART A: Fix DataExtensions.swift gradient drift

    The `openMatesOfficial` gradient in DataExtensions.swift currently uses hex values `0x6366F1` / `0x4F46E5`. The CSS source defines the openmates gradient as referencing the primary gradient, which uses `#4867cd` / `#5a85eb`. These are different colors (indigo vs blue).

    Update DataExtensions.swift:
    1. Change `openMatesOfficial` gradient to use `0x4867CD` / `0x5A85EB` (matching the primary gradient from gradients.yml and the generated `LinearGradient.primary`)
    2. Update the comment to indicate these values match `--color-primary-start` / `--color-primary-end` from theme.generated.css
    3. If `appOpenmates` and `appChat` were NOT generated by build-tokens.js in Task 1 (because the script doesn't handle `start_ref`/`end_ref`), add them to DataExtensions.swift:
       ```swift
       static let appOpenmates = omGradient(start: Color(hex: 0x4867CD), end: Color(hex: 0x5A85EB))
       static let appChat = omGradient(start: Color(hex: 0x4867CD), end: Color(hex: 0x5A85EB))
       ```
       With a comment: "// openmates and chat categories use the primary gradient — not generated by build-tokens.js because they use start_ref/end_ref"

    PART B: Verify Lexend Deca font registration

    1. Read FontRegistration.swift and verify it registers all 6 weights: Light, Regular, Medium, SemiBold, Bold, ExtraBold
    2. Verify each weight's TTF filename matches a file that exists in the Xcode project resources
    3. Check that the `fontFamily` property returns "Lexend Deca" (not any variant like "LexendDeca" or "Lexend Deca Variable")
    4. Verify TypographyTokens.generated.swift uses `Font.custom("Lexend Deca", size: N)` -- the string must match FontRegistration.swift's family name exactly
    5. Run: `grep -c "Lexend Deca" frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift` to confirm all 15 font size entries use "Lexend Deca"
    6. Check Info.plist for UIAppFonts entries listing all 6 TTF files:
       `grep -A 20 "UIAppFonts" apple/OpenMates/Info.plist` (or the appropriate plist path)

    If any font weight is missing from registration or Info.plist, add it. If the font family name string doesn't match between FontRegistration.swift and TypographyTokens.generated.swift, fix the mismatch.

    PART C: Run lint script

    Run the lint-swift-design-tokens.sh script against a sampling of UI files to confirm zero violations:
    ```
    for f in apple/OpenMates/Sources/Shared/Components/OMDesignPrimitives.swift apple/OpenMates/Sources/Shared/Components/OMButtonStyles.swift; do
      echo '{"tool_input":{"file_path":"'"$f"'"}}' | bash scripts/lint-swift-design-tokens.sh && echo "PASS: $f" || echo "FAIL: $f"
    done
    ```
  </action>
  <verify>
    <automated>cd /Users/kitty/git/OpenMates && grep "0x4867CD\|0x4867cd" apple/OpenMates/Sources/Shared/Extensions/DataExtensions.swift && echo "openMatesOfficial hex matches primary" && grep -c "Lexend Deca" apple/OpenMates/Sources/Shared/Extensions/FontRegistration.swift && grep -c 'Font.custom("Lexend Deca"' frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep "0x6366F1\|0x4F46E5" apple/OpenMates/Sources/Shared/Extensions/DataExtensions.swift` returns ZERO matches (old stale hex values removed)
    - `grep "0x4867CD\|0x4867cd" apple/OpenMates/Sources/Shared/Extensions/DataExtensions.swift` returns at least 1 match (correct primary hex present)
    - `grep -c "Lexend" apple/OpenMates/Sources/Shared/Extensions/FontRegistration.swift` returns >= 6 (one per weight registration)
    - `grep 'Font.custom("Lexend Deca"' frontend/packages/ui/src/tokens/generated/swift/TypographyTokens.generated.swift | wc -l` returns 15 (all 15 font sizes use correct family name)
    - The font family string in FontRegistration.swift's return value matches the string used in TypographyTokens.generated.swift (both "Lexend Deca")
    - Lint script exits 0 (no violations) when run against OMDesignPrimitives.swift and OMButtonStyles.swift
  </acceptance_criteria>
  <done>DataExtensions.swift openMatesOfficial gradient uses correct primary hex values matching CSS source. appOpenmates and appChat gradient entries exist in Swift (either generated or manually added). Font registration is verified complete for all 6 Lexend Deca weights with matching family name strings. Lint script passes on primitive component files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Token pipeline (CSS source -> build-tokens.js -> generated Swift) | Generated files must faithfully represent source values; drift = visual bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Generated token files hand-edited | mitigate | Regenerate from source via build-tokens.js; never hand-edit *.generated.swift |
| T-01-02 | Information Disclosure | Hex values in generated files | accept | Color hex values are not sensitive data; they are visual design tokens |
</threat_model>

<verification>
1. `cd frontend/packages/ui && npm run build:tokens` completes without error
2. `git diff frontend/packages/ui/src/tokens/generated/swift/` shows no unexpected changes after regeneration
3. All acceptance criteria in both tasks pass
4. `grep "0x6366F1" apple/OpenMates/Sources/Shared/Extensions/DataExtensions.swift` returns 0 matches
</verification>

<success_criteria>
- Generated Swift token files are confirmed in sync with CSS source (FOUN-01)
- Lexend Deca font registration is verified complete for all 6 weights (FOUN-03)
- lint-swift-design-tokens.sh passes with zero violations on primitive files
- DataExtensions.swift hand-maintained gradients use correct hex values matching CSS source
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

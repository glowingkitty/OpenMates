---
phase: 05-testing-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/apps/web_app/tests/multi-tab-encryption.spec.ts
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "A Playwright test opens two tabs (same BrowserContext) with the same chat and both tabs decrypt messages correctly"
    - "A Playwright test creates a chat in tab A, opens it in tab B, and verifies content decrypts correctly"
    - "All 14 regression fixture tests and 12 format validation tests pass with the post-rebuild code"
  artifacts:
    - path: "frontend/apps/web_app/tests/multi-tab-encryption.spec.ts"
      provides: "Multi-tab E2E encryption tests for TEST-01 and TEST-02"
      min_lines: 100
  key_links:
    - from: "multi-tab-encryption.spec.ts"
      to: "signup-flow-helpers"
      via: "require() for login helpers, TOTP, screenshots"
      pattern: "require.*signup-flow-helpers"
    - from: "multi-tab-encryption.spec.ts"
      to: "helpers/env-guard"
      via: "require() for skipWithoutCredentials"
      pattern: "require.*env-guard"
---

<objective>
Create the multi-tab E2E Playwright spec for TEST-01 and TEST-02, and verify TEST-03 (existing regression fixtures pass with post-rebuild code).

Purpose: Validates that the rebuilt encryption architecture works correctly across browser tabs sharing the same BrowserContext (same-device, same-user multi-tab scenario). Also confirms all historical encrypted formats still decrypt correctly after Phases 2-4.

Output: One new Playwright spec file with two test cases, plus confirmed passing of existing regression and format test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-testing-documentation/05-CONTEXT.md
@.planning/phases/05-testing-documentation/05-RESEARCH.md

<interfaces>
<!-- Existing test patterns the executor MUST follow -->

From frontend/apps/web_app/tests/multi-session-encryption.spec.ts (reference for patterns):
- Uses `const { test, expect, chromium } = require('@playwright/test');` (CommonJS)
- Uses `export {};` at top (Playwright Docker convention)
- Creates browser manually via `chromium.launch()` (not default fixtures)
- Uses `browser.newContext({ baseURL })` where baseURL from env
- Has `createSessionLogs()`, `attachListeners()`, `assertChatDecryptedCorrectly()` patterns
- KEY DIFFERENCE: existing spec uses TWO separate BrowserContexts (cross-device). New spec uses ONE context with TWO pages (same-device tabs).

From frontend/apps/web_app/tests/helpers/env-guard.ts:
```typescript
function skipWithoutCredentials(t, email, password, otpKey): void
// Usage: skipWithoutCredentials(test, TEST_EMAIL, TEST_PASSWORD, TEST_OTP_KEY);
```

From frontend/apps/web_app/tests/signup-flow-helpers (via multi-session spec):
```typescript
const { createSignupLogger, archiveExistingScreenshots, createStepScreenshotter,
        generateTotp, getTestAccount, getE2EDebugUrl } = require('./signup-flow-helpers');
const { email: TEST_EMAIL, password: TEST_PASSWORD, otpKey: TEST_OTP_KEY } = getTestAccount();
```

Key multi-session helpers to replicate/adapt:
- `loginToApp(page, logFn)` - full login flow with OTP
- `startNewChat(page, logFn)` - clicks new chat button
- `sendMessageAndGetChatId(page, message, logFn)` - types message, sends, extracts chat UUID from URL
- `waitForAssistantResponse(page, expectedText, logFn, timeoutMs)` - waits for AI reply
- `waitForChatInSidebarAndClick(page, titleFragment, logFn, timeoutMs)` - discovers chat in sidebar
- `assertChatDecryptedCorrectly(page, expectedText, label, logs, logFn)` - asserts no decrypt errors
- `deleteActiveChat(page, logFn)` - right-click delete

CRITICAL multi-tab difference (per D-01, Research Pitfall 1):
- Single BrowserContext = shared cookies, localStorage, IndexedDB
- Login ONLY in tab A. Tab B navigates to /chat directly (already authenticated)
- Do NOT log in twice — OTP rejected + session cookie conflict
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-tab encryption Playwright spec</name>
  <files>frontend/apps/web_app/tests/multi-tab-encryption.spec.ts</files>
  <read_first>
    frontend/apps/web_app/tests/multi-session-encryption.spec.ts
    frontend/apps/web_app/tests/helpers/env-guard.ts
    frontend/apps/web_app/tests/helpers/chat-test-helpers.ts
    frontend/apps/web_app/tests/signup-flow-helpers.js
    frontend/apps/web_app/playwright.config.ts
  </read_first>
  <action>
Create `frontend/apps/web_app/tests/multi-tab-encryption.spec.ts` per D-01 and D-02.

**File structure (CommonJS, matching existing spec patterns):**
```
export {};
const { test, expect, chromium } = require('@playwright/test');
const { skipWithoutCredentials } = require('./helpers/env-guard');
const { createSignupLogger, archiveExistingScreenshots, createStepScreenshotter, generateTotp, getTestAccount, getE2EDebugUrl } = require('./signup-flow-helpers');
```

**Header comment:** Explain this tests same-browser multi-tab (single BrowserContext, shared storage) vs multi-session-encryption.spec.ts which tests cross-browser (separate contexts, separate storage).

**Required env vars (same as multi-session spec):**
- `OPENMATES_TEST_ACCOUNT_EMAIL`
- `OPENMATES_TEST_ACCOUNT_PASSWORD`
- `OPENMATES_TEST_ACCOUNT_OTP_KEY`
- `PLAYWRIGHT_TEST_BASE_URL`

**Shared infrastructure (inline in file, adapted from multi-session spec):**
1. `SessionLogs` interface with `consoleLogs: string[]` and `decryptionErrors: string[]`
2. `createSessionLogs()` factory
3. `attachListeners(page, label, logs)` — captures console messages, flags decrypt errors
4. `loginToApp(page, logFn)` — full login flow (email, password, OTP, wait for /chat redirect)
5. `startNewChat(page, logFn)` — clicks `.icon_create`, waits 2s
6. `sendMessageAndGetChatId(page, message, logFn)` — types in `.editor-content.prose`, clicks `.send-button`, extracts UUID from URL
7. `waitForAssistantResponse(page, expectedText, logFn, timeoutMs)` — waits for `.message-wrapper.assistant` to contain text
8. `waitForChatInSidebarAndClick(page, titleFragment, logFn, timeoutMs)` — waits for `.chat-item-wrapper` with `.chat-title` matching fragment, clicks it
9. `assertChatDecryptedCorrectly(page, expectedText, label, logs, logFn)` — asserts last assistant message contains expected text, zero `decryptionErrors`, no error patterns in body text
10. `deleteActiveChat(page, logFn)` — right-click `.chat-item-wrapper.active`, click `.menu-item.delete` twice (enter confirm + confirm)

Copy these helpers from multi-session-encryption.spec.ts verbatim (they are inline there, not in shared helpers).

**Test 1 — TEST-01: "two tabs decrypt same chat correctly"**
```
test('TEST-01: two tabs open same chat, send messages, both tabs decrypt correctly', async () => {
  test.slow();
  test.setTimeout(300000); // 5 minutes
  skipWithoutCredentials(test, TEST_EMAIL, TEST_PASSWORD, TEST_OTP_KEY);
```

Flow:
1. Launch browser, create ONE BrowserContext with `{ baseURL }` — both tabs share storage
2. Create `tabA = await context.newPage()` and `tabB = await context.newPage()`
3. Attach console listeners to both tabs
4. Login in tab A only (full login flow)
5. Tab B navigates to `getE2EDebugUrl('/chat')` — already authenticated via shared cookies
6. Wait for tab B to reach `/chat` URL (timeout 15s)
7. In tab A: `startNewChat`, `sendMessageAndGetChatId(tabA, "Multi-tab test message from Tab A", logA)`
8. In tab A: `waitForAssistantResponse(tabA, ...)` — wait for AI response (use a regex or partial match, 60s timeout)
9. In tab B: `waitForChatInSidebarAndClick(tabB, "multi-tab", logB, 60000)` — discover chat via sidebar sync
10. Assert: `assertChatDecryptedCorrectly` on both tabA and tabB — no decrypt errors, assistant message visible
11. Cleanup: `deleteActiveChat(tabA, logA)`
12. Finally block: close context and browser

**Test 2 — TEST-02: "create in tab A, open in tab B, content decrypts"**
```
test('TEST-02: create chat in tab A, open in tab B, content decrypts correctly', async () => {
  test.slow();
  test.setTimeout(300000);
  skipWithoutCredentials(test, TEST_EMAIL, TEST_PASSWORD, TEST_OTP_KEY);
```

Flow:
1. Same setup: one BrowserContext, tabA + tabB, login only in tabA
2. Tab B navigates to `/chat` (authenticated via shared cookies)
3. In tab A: `startNewChat`, `sendMessageAndGetChatId(tabA, "Cross-tab creation test", logA)`
4. In tab A: `waitForAssistantResponse` (wait for AI, 60s)
5. In tab B: `waitForChatInSidebarAndClick(tabB, "cross-tab", logB, 60000)`
6. Assert: `assertChatDecryptedCorrectly` on tabB — the key insight: tab B never created this chat, it discovers it via sync and must decrypt with the shared key from IndexedDB/BroadcastChannel
7. Verify: tab B's `decryptionErrors` array is empty
8. Cleanup + finally block

**Error handling:** Both tests wrap in try/finally that closes context+browser. On assertion failure, take screenshots of both tabs before throwing.
  </action>
  <verify>
    <automated>cd /home/superdev/projects/OpenMates/frontend/apps/web_app && npx playwright test tests/multi-tab-encryption.spec.ts --list 2>&1 | grep -E "TEST-01|TEST-02"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/apps/web_app/tests/multi-tab-encryption.spec.ts` exists
    - File contains `export {};` on first non-comment line (Playwright Docker convention)
    - File contains `require('@playwright/test')` (CommonJS pattern)
    - File contains `TEST-01:` in a test name string
    - File contains `TEST-02:` in a test name string
    - File contains `context.newPage()` called twice (two tabs from one context)
    - File does NOT contain a second `loginToApp` call within the same test (only tab A logs in)
    - File contains `skipWithoutCredentials` call
    - File contains `assertChatDecryptedCorrectly` function definition or usage
    - `npx playwright test tests/multi-tab-encryption.spec.ts --list` shows both test names without errors
  </acceptance_criteria>
  <done>Two Playwright E2E test cases exist: TEST-01 (two tabs same chat) and TEST-02 (create in A, open in B). Both use single BrowserContext with two pages, login only once, and verify decryption correctness.</done>
</task>

<task type="auto">
  <name>Task 2: Verify TEST-03 — existing regression fixtures pass</name>
  <files></files>
  <read_first>
    frontend/packages/ui/src/services/encryption/__tests__/regression-fixtures.test.ts
    frontend/packages/ui/src/services/encryption/__tests__/formats.test.ts
  </read_first>
  <action>
Run the existing encryption test suites to confirm TEST-03 (all historical encrypted formats decrypt with post-rebuild code):

1. Run regression fixtures: `cd /home/superdev/projects/OpenMates/frontend/packages/ui && npx vitest run src/services/encryption/__tests__/regression-fixtures.test.ts`
2. Run format validation: `cd /home/superdev/projects/OpenMates/frontend/packages/ui && npx vitest run src/services/encryption/__tests__/formats.test.ts`
3. Run all encryption tests: `cd /home/superdev/projects/OpenMates/frontend/packages/ui && npx vitest run src/services/encryption/__tests__/`

If any tests fail, investigate and fix the failure — this would indicate a Phase 2-4 regression that must be resolved.

If all pass, TEST-03 is satisfied with zero new code needed. Document the passing test counts in the summary.
  </action>
  <verify>
    <automated>cd /home/superdev/projects/OpenMates/frontend/packages/ui && npx vitest run src/services/encryption/__tests__/regression-fixtures.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/services/encryption/__tests__/regression-fixtures.test.ts` exits with code 0
    - `npx vitest run src/services/encryption/__tests__/formats.test.ts` exits with code 0
    - Output shows 14+ tests passed in regression-fixtures
    - Output shows 12+ tests passed in formats
  </acceptance_criteria>
  <done>All historical encrypted format test fixtures (14 regression + 12 format) decrypt successfully with the post-Phase-4 rebuilt code. TEST-03 confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify multi-tab E2E tests and regression fixtures</name>
  <files></files>
  <action>Present the results of Tasks 1-2 for human verification. User runs the E2E tests against the dev server and confirms both TEST-01 and TEST-02 pass with no decryption errors.</action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <what-built>Multi-tab encryption E2E test spec (TEST-01 + TEST-02) and verified existing regression fixtures pass (TEST-03)</what-built>
  <how-to-verify>
    1. Review `frontend/apps/web_app/tests/multi-tab-encryption.spec.ts` — confirm it uses single BrowserContext with two pages (not two separate contexts like the multi-session spec)
    2. Run the E2E tests against the dev server (requires test account credentials):
       `cd frontend/apps/web_app && PLAYWRIGHT_TEST_BASE_URL=https://app.dev.openmates.org npx playwright test tests/multi-tab-encryption.spec.ts`
    3. Confirm both TEST-01 and TEST-02 pass
    4. Confirm regression fixture test output shows all 14+12 tests passing
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>User confirms E2E multi-tab tests pass against dev server and regression fixtures are green.</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/multi-tab-encryption.spec.ts --list` shows TEST-01 and TEST-02
- `npx vitest run src/services/encryption/__tests__/regression-fixtures.test.ts` passes (14 tests)
- `npx vitest run src/services/encryption/__tests__/formats.test.ts` passes (12 tests)
</verification>

<success_criteria>
- Two new E2E test cases exist testing multi-tab encryption scenarios
- Both tests use single BrowserContext (shared IndexedDB) with two pages
- All historical regression fixtures pass with post-rebuild code
- No new decryption errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/05-testing-documentation/05-01-SUMMARY.md`
</output>

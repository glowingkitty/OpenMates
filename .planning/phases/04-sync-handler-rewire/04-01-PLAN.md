---
phase: 04-sync-handler-rewire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/packages/ui/src/services/chatSyncServiceSenders.ts
  - frontend/packages/ui/src/services/sendersChatMessages.ts
  - frontend/packages/ui/src/services/sendersChatManagement.ts
  - frontend/packages/ui/src/services/sendersDrafts.ts
  - frontend/packages/ui/src/services/sendersEmbeds.ts
  - frontend/packages/ui/src/services/sendersSync.ts
autonomous: true
requirements: [ARCH-03]

must_haves:
  truths:
    - "chatSyncServiceSenders.ts is a ~30-line barrel of re-exports"
    - "All existing dynamic imports of chatSyncServiceSenders continue to work unchanged"
    - "No circular dependencies between the new sender sub-modules"
    - "Each new module is under 500 lines (ARCH-04 compliance)"
  artifacts:
    - path: "frontend/packages/ui/src/services/sendersChatMessages.ts"
      provides: "sendNewMessageImpl, sendEncryptedStoragePackage, sendCompletedAIResponseImpl"
      min_lines: 30
    - path: "frontend/packages/ui/src/services/sendersChatManagement.ts"
      provides: "sendUpdateTitleImpl, sendDeleteChatImpl, sendDeleteMessageImpl, sendUpdateChatKeyImpl, sendSetActiveChatImpl"
      min_lines: 30
    - path: "frontend/packages/ui/src/services/sendersDrafts.ts"
      provides: "sendUpdateDraftImpl, sendDeleteDraftImpl, sendDeleteDraftEmbedImpl"
      min_lines: 30
    - path: "frontend/packages/ui/src/services/sendersEmbeds.ts"
      provides: "sendStoreEmbedImpl, sendRequestEmbed, sendStoreEmbedKeysImpl"
      min_lines: 30
    - path: "frontend/packages/ui/src/services/sendersSync.ts"
      provides: "queueOfflineChangeImpl, sendOfflineChangesImpl, sendScrollPositionUpdateImpl, and misc senders"
      min_lines: 30
    - path: "frontend/packages/ui/src/services/chatSyncServiceSenders.ts"
      provides: "Barrel re-exports from all sub-modules"
      max_lines: 100
  key_links:
    - from: "frontend/packages/ui/src/services/chatSyncServiceSenders.ts"
      to: "sendersChatMessages.ts, sendersChatManagement.ts, sendersDrafts.ts, sendersEmbeds.ts, sendersSync.ts"
      via: "re-export statements"
      pattern: "export.*from.*senders"
---

<objective>
Split the 2851-line chatSyncServiceSenders.ts into 5 focused domain modules with a barrel re-export file for backwards compatibility.

Purpose: Per D-03, the oversized senders file must be decomposed before encrypt path conversion (Plan 03). The barrel re-export pattern (proven in Phase 2's cryptoService.ts split) ensures zero breaking changes at all call sites.
Output: 5 new sender sub-modules + slimmed barrel file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sync-handler-rewire/04-CONTEXT.md
@.planning/phases/04-sync-handler-rewire/04-RESEARCH.md

@frontend/packages/ui/src/services/chatSyncServiceSenders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract sender sub-modules from chatSyncServiceSenders.ts</name>
  <files>
    frontend/packages/ui/src/services/sendersChatMessages.ts,
    frontend/packages/ui/src/services/sendersChatManagement.ts,
    frontend/packages/ui/src/services/sendersDrafts.ts,
    frontend/packages/ui/src/services/sendersEmbeds.ts,
    frontend/packages/ui/src/services/sendersSync.ts
  </files>
  <read_first>
    frontend/packages/ui/src/services/chatSyncServiceSenders.ts
  </read_first>
  <action>
Split chatSyncServiceSenders.ts into 5 domain-focused modules following the boundaries identified in research:

1. **sendersChatMessages.ts** (~1400 lines): Move `sendNewMessageImpl`, `sendEncryptedStoragePackage`, `sendCompletedAIResponseImpl` and all their helper functions. This is the most complex module — contains critical-op lock usage (`acquireCriticalOp`/`releaseCriticalOp`) that MUST be preserved exactly (Pitfall 4 from research).

2. **sendersChatManagement.ts** (~400 lines): Move `sendUpdateTitleImpl`, `sendDeleteChatImpl`, `sendDeleteMessageImpl`, `sendUpdateChatKeyImpl`, `sendSetActiveChatImpl`.

3. **sendersDrafts.ts** (~150 lines): Move `sendUpdateDraftImpl`, `sendDeleteDraftImpl`, `sendDeleteDraftEmbedImpl`.

4. **sendersEmbeds.ts** (~200 lines): Move `sendStoreEmbedImpl`, `sendRequestEmbed`, `sendStoreEmbedKeysImpl`.

5. **sendersSync.ts** (~600 lines): Move `queueOfflineChangeImpl`, `sendOfflineChangesImpl`, `sendScrollPositionUpdateImpl`, `sendChatReadStatusImpl`, `sendPostProcessingMetadataImpl`, `sendLoadMoreChatsImpl`, `sendSyncInspirationChatImpl`, `sendCancelAiTaskImpl`, `sendCancelSkillImpl`, `sendCancelPdfProcessingImpl`, `sendAppSettingsMemoriesConfirmedImpl`, `sendStoreAppSettingsMemoriesEntryImpl`, `sendDeleteNewChatSuggestionImpl`, `sendDeleteNewChatSuggestionByIdImpl`.

For each new file:
- Add a 5-10 line file header comment explaining the module's domain
- Copy ALL imports needed by the functions being moved (do not miss any)
- If two sub-modules share a helper function or type, extract it to a `sendersTypes.ts` or `sendersUtils.ts` file to avoid circular imports (Pitfall 3 from research)
- Export every public function that was previously exported from chatSyncServiceSenders.ts
- Do NOT change any function signatures, logic, or import sources yet — this is a pure move refactor. Encrypt import conversions happen in Plan 03.
  </action>
  <verify>
    <automated>cd /home/superdev/projects/OpenMates/frontend/packages/ui && npx vitest run src/services/encryption/__tests__/ --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Each new file exists and has a header comment
    - grep -c "acquireCriticalOp" sendersChatMessages.ts returns at least 1
    - grep -c "releaseCriticalOp" sendersChatMessages.ts returns at least 1
    - wc -l on each new file shows all are under 1500 lines
    - No file imports from another sender sub-module (no circular deps)
  </acceptance_criteria>
  <done>5 new sender sub-modules exist with all 28 exported functions distributed by domain, critical-op lock preserved in sendersChatMessages.ts, no circular dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Convert chatSyncServiceSenders.ts to barrel re-export file</name>
  <files>
    frontend/packages/ui/src/services/chatSyncServiceSenders.ts
  </files>
  <read_first>
    frontend/packages/ui/src/services/chatSyncServiceSenders.ts,
    frontend/packages/ui/src/services/sendersChatMessages.ts,
    frontend/packages/ui/src/services/sendersChatManagement.ts,
    frontend/packages/ui/src/services/sendersDrafts.ts,
    frontend/packages/ui/src/services/sendersEmbeds.ts,
    frontend/packages/ui/src/services/sendersSync.ts
  </read_first>
  <action>
Replace the entire contents of chatSyncServiceSenders.ts with a barrel re-export file following the pattern proven in Phase 2's cryptoService.ts split:

```typescript
/**
 * chatSyncServiceSenders.ts — Barrel re-export for backwards compatibility
 *
 * All sender implementations have been split into focused domain modules.
 * This file re-exports everything so existing `import { ... } from "./chatSyncServiceSenders"`
 * and `await import("./chatSyncServiceSenders")` calls continue working unchanged.
 *
 * Modules:
 *   sendersChatMessages.ts — Message sending + encrypted storage
 *   sendersChatManagement.ts — Chat CRUD operations
 *   sendersDrafts.ts — Draft management
 *   sendersEmbeds.ts — Embed operations
 *   sendersSync.ts — Sync utilities + misc senders
 */
export * from "./sendersChatMessages";
export * from "./sendersChatManagement";
export * from "./sendersDrafts";
export * from "./sendersEmbeds";
export * from "./sendersSync";
```

Also re-export any types or interfaces that were exported from the original file.

After writing the barrel, verify that all dynamic imports still resolve by searching for `import("./chatSyncServiceSenders")` across the codebase — each one should still work because the barrel re-exports everything.

CRITICAL: The barrel must export the EXACT same set of names as the original file. Compare the export list before and after. Missing exports will cause runtime "X is not a function" errors (Pitfall 1 from research).
  </action>
  <verify>
    <automated>cd /home/superdev/projects/OpenMates/frontend/packages/ui && npx vitest run src/services/encryption/__tests__/ --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - wc -l chatSyncServiceSenders.ts shows under 100 lines
    - grep -c "export \* from" chatSyncServiceSenders.ts returns 5 (one per sub-module)
    - grep -rn 'import.*chatSyncServiceSenders' in services/ returns matches (proving existing imports exist)
    - All 90 encryption tests pass
  </acceptance_criteria>
  <done>chatSyncServiceSenders.ts is a slim barrel re-export file under 100 lines, all existing imports resolve, all 90 encryption tests pass</done>
</task>

</tasks>

<verification>
- All 90 encryption tests pass: `cd frontend/packages/ui && npx vitest run src/services/encryption/__tests__/ --reporter=verbose`
- No runtime import errors: `grep -rn 'import.*chatSyncServiceSenders' frontend/packages/ui/src/services/` shows existing imports unchanged
- Barrel exports match: compare export count of barrel vs sum of sub-module exports
- ARCH-04 compliance: `wc -l` on each new module shows under 500 lines (except sendersChatMessages which may be ~1400 — acceptable as it is not an encryption module)
</verification>

<success_criteria>
chatSyncServiceSenders.ts is decomposed into 5 focused modules with a barrel re-export, zero breaking changes at call sites, all 90 encryption tests pass, and no circular dependencies between sub-modules.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sync-handler-rewire/04-01-SUMMARY.md`
</output>

---
phase: 07-e2e-test-suite-repair
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/apps/web_app/tests/skill-web-search.spec.ts
  - frontend/apps/web_app/tests/skill-news-search.spec.ts
  - frontend/apps/web_app/tests/skill-code-docs.spec.ts
  - frontend/apps/web_app/tests/skill-events-search.spec.ts
  - frontend/apps/web_app/tests/skill-health-appointments.spec.ts
  - frontend/apps/web_app/tests/skill-images-search.spec.ts
  - frontend/apps/web_app/tests/skill-maps-search.spec.ts
  - frontend/apps/web_app/tests/skill-math-calculate.spec.ts
  - frontend/apps/web_app/tests/skill-reminder-set.spec.ts
  - frontend/apps/web_app/tests/skill-shopping-search.spec.ts
  - frontend/apps/web_app/tests/skill-travel-connections.spec.ts
  - frontend/apps/web_app/tests/skill-travel-stays.spec.ts
  - frontend/apps/web_app/tests/skill-videos-search.spec.ts
  - frontend/apps/web_app/tests/skill-videos-transcript.spec.ts
  - frontend/apps/web_app/tests/skill-web-read.spec.ts
  - frontend/apps/web_app/tests/helpers/embed-test-helpers.ts
  - frontend/apps/web_app/tests/helpers/chat-test-helpers.ts
  - frontend/apps/web_app/tests/signup-flow-helpers.ts
  - frontend/apps/web_app/tests/console-monitor.ts
autonomous: true
requirements: [E2E-01]

must_haves:
  truths:
    - "All 15 skill-* specs pass on GHA"
    - "No passing spec was modified or broken"
  artifacts:
    - path: "frontend/apps/web_app/tests/skill-web-search.spec.ts"
      provides: "Fixed skill spec (representative)"
    - path: "frontend/apps/web_app/tests/helpers/embed-test-helpers.ts"
      provides: "Shared embed helpers (possibly updated)"
  key_links:
    - from: ".planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md"
      to: "skill-*.spec.ts fixes"
      via: "Triage category for skill specs"
      pattern: "Category.*skill"
---

<objective>
Fix all 15 failing skill-* Playwright specs based on the root cause identified in Plan 01 triage.

Purpose: The 15 skill specs are NEW failures (appeared between 03-24 and 03-25 runs). They all share identical structure (4-phase pattern) and identical imports. Per user decision: if triage confirms same root cause, apply a single batch fix to all 15.

Output: All 15 skill-* specs passing on GHA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e2e-test-suite-repair/07-CONTEXT.md
@.planning/phases/07-e2e-test-suite-repair/07-RESEARCH.md
@.planning/phases/07-e2e-test-suite-repair/07-01-SUMMARY.md
@.planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md

<interfaces>
<!-- All 15 skill specs import from these 5 shared helpers -->
From frontend/apps/web_app/tests/console-monitor.ts:
  exports: test, expect (wrapped @playwright/test with console error monitoring)

From frontend/apps/web_app/tests/signup-flow-helpers.ts:
  exports: createSignupLogger, archiveExistingScreenshots, createStepScreenshotter,
           getTestAccount, withLiveMockMarker, generateTotp, getE2EDebugUrl

From frontend/apps/web_app/tests/helpers/chat-test-helpers.ts:
  exports: loginToTestAccount, startNewChat, sendMessage, deleteActiveChat

From frontend/apps/web_app/tests/helpers/cli-test-helpers.ts:
  exports: deriveApiUrl, runCli, parseCliJson

From frontend/apps/web_app/tests/helpers/embed-test-helpers.ts:
  exports: verifyEmbedPreviewPage, waitForEmbedFinished, openFullscreen,
           verifySearchGrid, closeFullscreen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply batch fix to all 15 skill-* specs based on triage root cause</name>
  <read_first>
    - .planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md (root cause category for skill specs)
    - .planning/phases/07-e2e-test-suite-repair/07-01-SUMMARY.md (triage findings)
    - test-results/triage-07-01.json (actual error messages for skill-web-search and skill-news-search)
    - frontend/apps/web_app/tests/skill-web-search.spec.ts (representative spec to understand structure)
    - frontend/apps/web_app/tests/helpers/chat-test-helpers.ts (login helper -- may need fixes)
    - frontend/apps/web_app/tests/helpers/embed-test-helpers.ts (embed helpers -- may need fixes)
    - frontend/apps/web_app/tests/signup-flow-helpers.ts (logger helpers -- recently changed, potential issue source)
    - frontend/apps/web_app/tests/console-monitor.ts (console monitor -- potential issue source)
  </read_first>
  <files>
    frontend/apps/web_app/tests/skill-web-search.spec.ts
    frontend/apps/web_app/tests/skill-news-search.spec.ts
    frontend/apps/web_app/tests/skill-code-docs.spec.ts
    frontend/apps/web_app/tests/skill-events-search.spec.ts
    frontend/apps/web_app/tests/skill-health-appointments.spec.ts
    frontend/apps/web_app/tests/skill-images-search.spec.ts
    frontend/apps/web_app/tests/skill-maps-search.spec.ts
    frontend/apps/web_app/tests/skill-math-calculate.spec.ts
    frontend/apps/web_app/tests/skill-reminder-set.spec.ts
    frontend/apps/web_app/tests/skill-shopping-search.spec.ts
    frontend/apps/web_app/tests/skill-travel-connections.spec.ts
    frontend/apps/web_app/tests/skill-travel-stays.spec.ts
    frontend/apps/web_app/tests/skill-videos-search.spec.ts
    frontend/apps/web_app/tests/skill-videos-transcript.spec.ts
    frontend/apps/web_app/tests/skill-web-read.spec.ts
    frontend/apps/web_app/tests/helpers/embed-test-helpers.ts
    frontend/apps/web_app/tests/helpers/chat-test-helpers.ts
    frontend/apps/web_app/tests/signup-flow-helpers.ts
    frontend/apps/web_app/tests/console-monitor.ts
  </files>
  <action>
    Read 07-01-TRIAGE.md to get the confirmed root cause for skill-* specs.

    **If same root cause for both triaged skill specs (expected):**
    Apply the fix to all 15 skill specs at once. The fix depends on the triage findings:

    - **If logger/import issue** (commits 16a2e8baa, 308c02003 introduced breakage): Fix the `createSignupLogger` usage or import path in all 15 specs. The recent commits "fix: correct createSignupLogger usage in 15 skill specs" and "fix: repair 15 skill spec screenshot logger calls" suggest the logger API changed -- verify the current function signature in `signup-flow-helpers.ts` matches how specs call it.

    - **If embed preview page changed** (Phase 1 fails): Update `verifyEmbedPreviewPage()` in `embed-test-helpers.ts` to match the current `/dev/preview/embeds/{app}` page structure. Check what the page actually renders by examining the route handler.

    - **If login flow timing** (Phase 4 fails): The shared `loginToTestAccount()` in `chat-test-helpers.ts` should handle this, but verify the specs are using it correctly. Check timeouts and selector fallbacks.

    - **If console.error monitor triggers** (BENIGN_ERROR_PATTERNS too strict): Update the allowlist in `console-monitor.ts` to include any new benign errors that appear on GHA.

    **If different root causes for the two triaged skill specs:**
    Fix each individual spec based on its specific error. Group specs by sub-category if patterns emerge.

    IMPORTANT per user decisions:
    - Only modify the 15 failing skill-* specs and their shared helpers
    - Do NOT modify any of the 39 passing specs
    - If fixing a shared helper, verify the fix doesn't break passing specs that use the same helper
  </action>
  <verify>
    <automated>python3 -c "
import subprocess, json
# Verify no syntax errors in modified files
specs = ['skill-web-search', 'skill-news-search']
for s in specs:
    r = subprocess.run(['node', '-e', f'require(\"./frontend/apps/web_app/tests/{s}.spec.ts\")'], capture_output=True, text=True)
    # TypeScript files won't run directly in node, but we can check they parse
print('Syntax check: manual verification via GHA run required')
print('Dispatch 2 skill specs to GHA to verify fix')
"</automated>
  </verify>
  <done>All 15 skill-* spec files are updated with the fix identified from triage. The fix addresses the confirmed root cause.</done>
</task>

<task type="auto">
  <name>Task 2: Verify skill-* fix by running 3 representative specs on GHA</name>
  <read_first>
    - frontend/apps/web_app/tests/skill-web-search.spec.ts (verify fix was applied)
    - frontend/apps/web_app/tests/skill-math-calculate.spec.ts (different app type for broader coverage)
    - frontend/apps/web_app/tests/skill-travel-stays.spec.ts (third sample)
    - scripts/run_tests.py (how to dispatch and check results)
  </read_first>
  <files>test-results/triage-07-01.json</files>
  <action>
    After deploying the fix from Task 1, run 3 representative skill specs on GHA to confirm the fix works:

    1. `skill-web-search.spec.ts` (was triaged -- should now pass)
    2. `skill-math-calculate.spec.ts` (different app type)
    3. `skill-travel-stays.spec.ts` (third app type)

    Dispatch each:
    ```bash
    gh workflow run playwright-spec.yml \
      --repo glowingkitty/OpenMates \
      --ref dev \
      -f spec={spec} \
      -f account=1 \
      -f use_mocks=true
    ```

    Poll for completion and check results:
    ```bash
    gh run list --workflow=playwright-spec.yml --limit 3 --json databaseId,conclusion
    ```

    If any fail, download logs and iterate on the fix:
    ```bash
    gh run view {run_id} --log | grep -A 20 "Error\|FAILED"
    ```

    If all 3 pass, the batch fix is validated. Update triage-07-01.json with verification results.

    If a spec still fails, diagnose the specific error, apply a targeted fix, re-deploy, and re-run on GHA.
  </action>
  <verify>
    <automated>python3 -c "
# Verify by checking GHA run conclusions for the 3 verification specs
# This will be checked by examining the GHA run results after dispatch
print('Verify: 3 skill specs must show conclusion=success on GHA')
print('Run: gh run list --workflow=playwright-spec.yml --limit 3 --json conclusion')
"</automated>
  </verify>
  <done>At least 3 representative skill-* specs pass on GHA after the fix, confirming the batch fix works for all 15.</done>
</task>

</tasks>

<verification>
- At least 3 of 3 verification skill specs pass on GHA
- No previously passing spec was modified
- Changes are deployed to dev branch via sessions.py deploy
- If shared helpers were modified, a passing spec that uses the same helper still passes
</verification>

<success_criteria>
- All 15 skill-* specs have the identified fix applied
- 3 representative specs confirmed passing on GHA
- E2E-01 requirement is satisfied (pending full daily run confirmation in Plan 04)
</success_criteria>

<output>
After completion, create `.planning/phases/07-e2e-test-suite-repair/07-02-SUMMARY.md`
</output>

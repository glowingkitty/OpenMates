---
phase: 07-e2e-test-suite-repair
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/apps/web_app/tests/signup-flow.spec.ts
  - frontend/apps/web_app/tests/signup-flow-passkey.spec.ts
  - frontend/apps/web_app/tests/signup-flow-polar.spec.ts
  - frontend/apps/web_app/tests/signup-skip-2fa-flow.spec.ts
  - frontend/apps/web_app/tests/api-keys-flow.spec.ts
  - frontend/apps/web_app/tests/backup-code-login-flow.spec.ts
  - frontend/apps/web_app/tests/backup-codes-settings.spec.ts
  - frontend/apps/web_app/tests/background-chat-notification.spec.ts
  - frontend/apps/web_app/tests/cli-file-upload.spec.ts
  - frontend/apps/web_app/tests/code-generation-multiturn.spec.ts
  - frontend/apps/web_app/tests/connection-resilience.spec.ts
  - frontend/apps/web_app/tests/daily-inspiration-chat-flow.spec.ts
  - frontend/apps/web_app/tests/default-model-settings.spec.ts
  - frontend/apps/web_app/tests/message-sync.spec.ts
  - frontend/apps/web_app/tests/model-override.spec.ts
  - frontend/apps/web_app/tests/multi-session-encryption.spec.ts
  - frontend/apps/web_app/tests/newsletter-flow.spec.ts
  - frontend/apps/web_app/tests/pdf-flow.spec.ts
  - frontend/apps/web_app/tests/recent-chats-dedup.spec.ts
  - frontend/apps/web_app/tests/settings-buy-credits-polar.spec.ts
  - frontend/apps/web_app/tests/settings-buy-credits-stripe.spec.ts
  - frontend/apps/web_app/tests/share-chat-flow.spec.ts
  - frontend/apps/web_app/tests/share-embed-flow.spec.ts
  - frontend/apps/web_app/tests/shared-chat-open.spec.ts
  - frontend/apps/web_app/tests/reminder-redesign.spec.ts
  - frontend/apps/web_app/tests/signup-flow-helpers.ts
  - frontend/apps/web_app/tests/helpers/chat-test-helpers.ts
autonomous: true
requirements: [E2E-02, E2E-03]

must_haves:
  truths:
    - "All 4 signup-* specs pass on GHA"
    - "Login-dependent 'other' specs pass on GHA"
    - "No passing spec was broken"
  artifacts:
    - path: "frontend/apps/web_app/tests/signup-flow.spec.ts"
      provides: "Fixed signup spec (representative)"
    - path: "frontend/apps/web_app/tests/api-keys-flow.spec.ts"
      provides: "Fixed login-dependent spec (representative)"
  key_links:
    - from: ".planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md"
      to: "signup + auth-dependent spec fixes"
      via: "Triage categories for signup and login-dependent failures"
      pattern: "Category.*signup|login"
---

<objective>
Fix all 4 signup-* specs and the ~20 login/auth-dependent "other" specs based on triage root causes from Plan 01.

Purpose: These specs cover signup flows (Mailosaur email verification, Stripe checkout, passkey registration) and authenticated features (settings, chat operations, API keys). Fixing them addresses E2E-02 and the auth-dependent portion of E2E-03.

Output: Signup and auth-dependent specs passing on GHA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e2e-test-suite-repair/07-CONTEXT.md
@.planning/phases/07-e2e-test-suite-repair/07-RESEARCH.md
@.planning/phases/07-e2e-test-suite-repair/07-01-SUMMARY.md
@.planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md

<interfaces>
From frontend/apps/web_app/tests/signup-flow-helpers.ts:
  exports: createSignupLogger, archiveExistingScreenshots, createStepScreenshotter,
           getTestAccount, withLiveMockMarker, generateTotp, getE2EDebugUrl,
           checkMailosaurQuota, waitForMailosaurEmail, verifyEmailWithOtp

From frontend/apps/web_app/tests/helpers/chat-test-helpers.ts:
  exports: loginToTestAccount, startNewChat, sendMessage, deleteActiveChat
  loginToTestAccount signature: (page, logCheckpoint?, takeStepScreenshot?, options?) => Promise<void>
  Login flow: goto -> click Login button -> fill email -> click Continue -> fill password -> fill OTP -> submit -> wait for editor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix signup-* specs (4 specs) based on triage root cause</name>
  <read_first>
    - .planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md (root cause for signup category)
    - test-results/triage-07-01.json (actual error for signup-flow.spec.ts)
    - frontend/apps/web_app/tests/signup-flow.spec.ts (main signup spec)
    - frontend/apps/web_app/tests/signup-flow-passkey.spec.ts (passkey variant)
    - frontend/apps/web_app/tests/signup-flow-polar.spec.ts (Polar payment variant)
    - frontend/apps/web_app/tests/signup-skip-2fa-flow.spec.ts (skip-2FA variant)
    - frontend/apps/web_app/tests/signup-flow-helpers.ts (shared helpers -- potential fix location)
  </read_first>
  <files>
    frontend/apps/web_app/tests/signup-flow.spec.ts
    frontend/apps/web_app/tests/signup-flow-passkey.spec.ts
    frontend/apps/web_app/tests/signup-flow-polar.spec.ts
    frontend/apps/web_app/tests/signup-skip-2fa-flow.spec.ts
    frontend/apps/web_app/tests/signup-flow-helpers.ts
  </files>
  <action>
    Read the triage document for the signup category root cause. Apply fixes based on the confirmed error:

    **Likely fixes based on research findings:**

    - **If Mailosaur timeout** (240s/300s exceeded): Increase test timeout, add retry logic for email polling, or check Mailosaur quota with `checkMailosaurQuota()` before the test runs.

    - **If selector drift in signup UI**: Update selectors in the spec to match current DOM structure. Check the signup form component for recent changes to input IDs, button text, or form structure.

    - **If Stripe/Polar iframe timing**: Add explicit frame locator waits before interacting with payment iframes. Stripe iframes load asynchronously and may need `page.frameLocator('iframe[name*="stripe"]')` with extended timeouts.

    - **If logger/helper function signature mismatch** (same issue as skill specs): Fix the function call signatures to match current `signup-flow-helpers.ts` exports.

    Apply the fix to all 4 signup specs. If the fix is in `signup-flow-helpers.ts`, verify passing specs that import from it are not affected (check the 39 passing spec list -- none start with `signup-` so the helpers are safe to modify for signup-specific functions).

    IMPORTANT: Only fix the 4 failing signup specs. Do NOT touch passing specs.
    IMPORTANT: If modifying signup-flow-helpers.ts, verify that functions used by passing specs (like `getTestAccount`, `generateTotp`, `getE2EDebugUrl`) are not changed.

    After fixing, deploy via `sessions.py deploy` and run 1 signup spec on GHA to verify:
    ```bash
    gh workflow run playwright-spec.yml --repo glowingkitty/OpenMates --ref dev \
      -f spec=signup-flow.spec.ts -f account=1 -f use_mocks=true
    ```
  </action>
  <verify>
    <automated>python3 -c "
# Verify signup specs are syntactically valid after changes
import subprocess
specs = ['signup-flow', 'signup-flow-passkey', 'signup-flow-polar', 'signup-skip-2fa-flow']
for s in specs:
    path = f'frontend/apps/web_app/tests/{s}.spec.ts'
    with open(path) as f:
        content = f.read()
    assert 'test(' in content or 'test.describe(' in content, f'{s} missing test definition'
print('OK: All 4 signup specs have valid test structures')
"</automated>
  </verify>
  <done>All 4 signup-* specs have fixes applied based on triage root cause. At least 1 signup spec verified passing on GHA.</done>
</task>

<task type="auto">
  <name>Task 2: Fix login-dependent "other" specs (~20 specs) by root cause batch</name>
  <read_first>
    - .planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md (categories for other specs)
    - test-results/triage-07-01.json (actual error for api-keys-flow.spec.ts)
    - frontend/apps/web_app/tests/api-keys-flow.spec.ts (triaged login-dependent spec)
    - frontend/apps/web_app/tests/backup-codes-settings.spec.ts (another login-dependent spec)
    - frontend/apps/web_app/tests/default-model-settings.spec.ts (settings spec)
    - frontend/apps/web_app/tests/share-chat-flow.spec.ts (chat operation spec)
    - frontend/apps/web_app/tests/helpers/chat-test-helpers.ts (shared login helper)
    - frontend/apps/web_app/tests/connection-resilience.spec.ts (WebSocket spec)
    - frontend/apps/web_app/tests/message-sync.spec.ts (sync spec)
    - frontend/apps/web_app/tests/multi-session-encryption.spec.ts (encryption spec)
  </read_first>
  <files>
    frontend/apps/web_app/tests/api-keys-flow.spec.ts
    frontend/apps/web_app/tests/backup-code-login-flow.spec.ts
    frontend/apps/web_app/tests/backup-codes-settings.spec.ts
    frontend/apps/web_app/tests/background-chat-notification.spec.ts
    frontend/apps/web_app/tests/cli-file-upload.spec.ts
    frontend/apps/web_app/tests/code-generation-multiturn.spec.ts
    frontend/apps/web_app/tests/connection-resilience.spec.ts
    frontend/apps/web_app/tests/daily-inspiration-chat-flow.spec.ts
    frontend/apps/web_app/tests/default-model-settings.spec.ts
    frontend/apps/web_app/tests/message-sync.spec.ts
    frontend/apps/web_app/tests/model-override.spec.ts
    frontend/apps/web_app/tests/multi-session-encryption.spec.ts
    frontend/apps/web_app/tests/newsletter-flow.spec.ts
    frontend/apps/web_app/tests/pdf-flow.spec.ts
    frontend/apps/web_app/tests/recent-chats-dedup.spec.ts
    frontend/apps/web_app/tests/settings-buy-credits-polar.spec.ts
    frontend/apps/web_app/tests/settings-buy-credits-stripe.spec.ts
    frontend/apps/web_app/tests/share-chat-flow.spec.ts
    frontend/apps/web_app/tests/share-embed-flow.spec.ts
    frontend/apps/web_app/tests/shared-chat-open.spec.ts
    frontend/apps/web_app/tests/helpers/chat-test-helpers.ts
  </files>
  <action>
    Read the triage document for the login-dependent and auth-dependent spec categories. Group specs by their specific sub-category from triage and apply batch fixes.

    **For each sub-category from triage, apply the appropriate fix:**

    - **If specs use inline login instead of `loginToTestAccount()`**: Migrate them to use the shared helper. The shared helper has OTP retry logic, fallback selectors, and 15s timeouts that inline code often lacks.

    - **If specs use `loginToTestAccount()` but still fail on login timing**: Check if the spec overrides timeouts or adds actions between login steps that could interfere. Ensure the spec waits for `loginToTestAccount()` to fully complete before proceeding.

    - **If specs fail on post-login selectors** (feature drift): Update the stale selectors to match current DOM. Prefer `data-testid` or `getByRole()` over CSS class selectors. Check the relevant Svelte component for the current attribute/class names.

    - **If specs fail on console.error monitor** (false positives): Add the specific error pattern to `BENIGN_ERROR_PATTERNS` in `console-monitor.ts` (only if the error is genuinely benign -- not a real bug).

    - **If newsletter-flow fails on Mailosaur**: Same fix pattern as signup specs (timeout increase, quota check).

    Process:
    1. Read each failing spec to identify its login pattern (grep for `loginToTestAccount` vs inline login code)
    2. Group by error pattern from triage
    3. Apply batch fix per group
    4. Deploy and verify 2 representative specs on GHA

    After fixing, deploy and run 2 specs:
    ```bash
    gh workflow run playwright-spec.yml --repo glowingkitty/OpenMates --ref dev \
      -f spec=api-keys-flow.spec.ts -f account=1 -f use_mocks=true
    gh workflow run playwright-spec.yml --repo glowingkitty/OpenMates --ref dev \
      -f spec=default-model-settings.spec.ts -f account=1 -f use_mocks=true
    ```

    IMPORTANT: Only fix the failing specs listed above. Do NOT modify any of the 39 passing specs.
    IMPORTANT: If modifying `chat-test-helpers.ts`, verify passing specs that use it (chat-flow, embed-json-leak, follow-up-suggestions-flow, etc.) are not affected.
  </action>
  <verify>
    <automated>python3 -c "
# Verify all modified specs still have valid test structures
import os
specs_dir = 'frontend/apps/web_app/tests'
other_specs = [
    'api-keys-flow', 'backup-code-login-flow', 'backup-codes-settings',
    'background-chat-notification', 'code-generation-multiturn', 'connection-resilience',
    'daily-inspiration-chat-flow', 'default-model-settings', 'message-sync',
    'model-override', 'multi-session-encryption', 'share-chat-flow'
]
for s in other_specs:
    path = os.path.join(specs_dir, f'{s}.spec.ts')
    with open(path) as f:
        content = f.read()
    assert 'test(' in content or 'test.describe(' in content, f'{s} missing test definition'
print(f'OK: {len(other_specs)} specs have valid test structures')
"</automated>
  </verify>
  <done>Login-dependent "other" specs are fixed with appropriate batch fixes per triage sub-category. At least 2 representative specs verified passing on GHA.</done>
</task>

</tasks>

<verification>
- At least 1 signup spec passes on GHA
- At least 2 login-dependent other specs pass on GHA
- No previously passing spec was modified
- All changes deployed via sessions.py deploy
</verification>

<success_criteria>
- All 4 signup-* specs have fixes applied (E2E-02)
- ~20 login-dependent other specs have fixes applied (partial E2E-03)
- Representative specs verified passing on GHA
</success_criteria>

<output>
After completion, create `.planning/phases/07-e2e-test-suite-repair/07-03-SUMMARY.md`
</output>

---
phase: 07-e2e-test-suite-repair
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - frontend/apps/web_app/tests/a11y-pages.spec.ts
  - frontend/apps/web_app/tests/docs-links.spec.ts
  - frontend/apps/web_app/tests/embed-showcase.spec.ts
  - frontend/apps/web_app/tests/not-found-404-flow.spec.ts
  - frontend/apps/web_app/tests/preview-error.spec.ts
  - frontend/apps/web_app/tests/seo-demo-chat.spec.ts
  - frontend/apps/web_app/tests/status-page.spec.ts
  - test-results/daily-run-validation.json
autonomous: false
requirements: [E2E-03, E2E-04]

must_haves:
  truths:
    - "Remaining ~7 non-auth specs pass on GHA"
    - "Full daily suite achieves 85+ of 88 passing"
    - "No passing spec was broken by any fix across Plans 02-04"
  artifacts:
    - path: "test-results/daily-run-validation.json"
      provides: "Full daily run results showing 85+/88 pass rate"
  key_links:
    - from: "All 46 previously failing specs"
      to: "test-results/daily-run-validation.json"
      via: "Full daily run via run_tests.py --daily"
      pattern: "passed.*8[5-9]|passed.*88"
---

<objective>
Fix the remaining ~7 non-auth-dependent failing specs and run a full daily validation to confirm 85+/88 pass rate.

Purpose: After Plans 02 and 03 fix the skill, signup, and login-dependent specs, a handful of non-login specs remain (status page, 404 handling, embed showcase, docs links, etc.). This plan fixes those stragglers and validates the complete suite meets the E2E-04 target of 85+ passing specs.

Output: Full daily run achieving 85+/88 pass rate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e2e-test-suite-repair/07-CONTEXT.md
@.planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md
@.planning/phases/07-e2e-test-suite-repair/07-02-SUMMARY.md
@.planning/phases/07-e2e-test-suite-repair/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix remaining non-auth-dependent failing specs (~7 specs)</name>
  <read_first>
    - .planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md (root causes for non-auth specs)
    - test-results/triage-07-01.json (actual errors for status-page and not-found-404-flow)
    - frontend/apps/web_app/tests/status-page.spec.ts (triaged non-login spec)
    - frontend/apps/web_app/tests/not-found-404-flow.spec.ts (triaged non-login spec)
    - frontend/apps/web_app/tests/a11y-pages.spec.ts (a11y scan spec)
    - frontend/apps/web_app/tests/docs-links.spec.ts (docs link checking spec)
    - frontend/apps/web_app/tests/embed-showcase.spec.ts (embed preview spec)
    - frontend/apps/web_app/tests/preview-error.spec.ts (dev preview error spec)
    - frontend/apps/web_app/tests/seo-demo-chat.spec.ts (SSR content spec)
  </read_first>
  <files>
    frontend/apps/web_app/tests/a11y-pages.spec.ts
    frontend/apps/web_app/tests/docs-links.spec.ts
    frontend/apps/web_app/tests/embed-showcase.spec.ts
    frontend/apps/web_app/tests/not-found-404-flow.spec.ts
    frontend/apps/web_app/tests/preview-error.spec.ts
    frontend/apps/web_app/tests/seo-demo-chat.spec.ts
    frontend/apps/web_app/tests/status-page.spec.ts
  </files>
  <action>
    Read triage root causes for each non-auth spec. These specs do NOT require login, so their failures are likely:

    - **Stale selectors** (UI components changed, CSS classes renamed)
    - **Route changes** (pages moved or restructured)
    - **Content changes** (expected text no longer matches)
    - **Console error monitor** (new console.error messages on these pages)
    - **Same structural issue** as skill/signup specs if the root cause was import/module-level

    For each spec:
    1. Read the spec to understand what it tests
    2. Check the triage category and error message
    3. Apply the specific fix

    **Likely individual fixes:**
    - `status-page.spec.ts`: Check if `/status` route exists and renders expected elements. Update selectors to match current page structure.
    - `not-found-404-flow.spec.ts`: Check if 404 page renders `.not-found-screen` or if the class changed. Update selector.
    - `a11y-pages.spec.ts`: This spec runs axe-core scans. If it needs login for some pages, check if login is failing. If axe violations are the issue, that's a real bug (don't suppress -- report as known issue).
    - `docs-links.spec.ts`: Check if `/docs/` route structure changed. Update expected links/paths.
    - `embed-showcase.spec.ts`: Similar to skill spec Phase 1 -- checks `/dev/preview/embeds/` pages. May have same fix as embed-test-helpers changes from Plan 02.
    - `preview-error.spec.ts`: Check `/dev/preview/` error handling routes.
    - `seo-demo-chat.spec.ts`: Check `/demo/chat/` SSR content expectations.

    NOTE: If any of these specs share the SAME root cause as the skill/signup specs (e.g., a module-level import error), they may already be fixed by Plans 02/03 changes. Check triage data first.

    After fixing, deploy and run 2 non-auth specs on GHA:
    ```bash
    gh workflow run playwright-spec.yml --repo glowingkitty/OpenMates --ref dev \
      -f spec=status-page.spec.ts -f account=1 -f use_mocks=true
    gh workflow run playwright-spec.yml --repo glowingkitty/OpenMates --ref dev \
      -f spec=not-found-404-flow.spec.ts -f account=1 -f use_mocks=true
    ```

    IMPORTANT: Only fix the 7 non-auth failing specs. Do NOT touch passing specs.
  </action>
  <verify>
    <automated>python3 -c "
import os
specs = ['a11y-pages', 'docs-links', 'embed-showcase', 'not-found-404-flow', 'preview-error', 'seo-demo-chat', 'status-page']
for s in specs:
    path = f'frontend/apps/web_app/tests/{s}.spec.ts'
    assert os.path.exists(path), f'{path} does not exist'
    with open(path) as f:
        content = f.read()
    assert 'test(' in content or 'test.describe(' in content, f'{s} missing test definition'
print(f'OK: All {len(specs)} non-auth specs have valid test structures')
"</automated>
  </verify>
  <done>All ~7 non-auth-dependent specs have fixes applied. At least 2 verified passing on GHA.</done>
</task>

<task type="auto">
  <name>Task 2: Run full daily suite and fix any remaining failures to reach 85+/88</name>
  <read_first>
    - scripts/run_tests.py (how to trigger daily run and check results)
    - .planning/phases/07-e2e-test-suite-repair/07-02-SUMMARY.md (skill spec fix results)
    - .planning/phases/07-e2e-test-suite-repair/07-03-SUMMARY.md (signup + auth spec fix results)
    - test-results/daily-run-2026-03-25.json (baseline: 42/88 passing)
  </read_first>
  <files>test-results/daily-run-validation.json</files>
  <action>
    Run the full daily suite on GHA to get a comprehensive pass/fail count:

    ```bash
    python3 scripts/run_tests.py --suite playwright --no-fail-fast
    ```

    Wait for all 88 specs to complete. The results file will be saved automatically by run_tests.py.

    Copy the results to the validation file:
    ```bash
    cp test-results/daily-run-$(date +%Y-%m-%d).json test-results/daily-run-validation.json
    ```

    Check the pass rate:
    ```python
    import json
    data = json.load(open('test-results/daily-run-validation.json'))
    pw = data['suites']['playwright']
    passed = sum(1 for t in pw['tests'] if t['status'] == 'passed')
    failed = sum(1 for t in pw['tests'] if t['status'] == 'failed')
    print(f'Pass rate: {passed}/{passed+failed}')
    ```

    **If pass rate is below 85/88 (3 or fewer failures allowed):**
    1. Identify which specs still fail
    2. Download their GHA logs to get error messages
    3. Apply targeted fixes for each remaining failure
    4. Deploy and re-run only the fixed specs to verify
    5. If still below target after 2 fix iterations, document the remaining failures as known issues with reasons (e.g., "requires Mailosaur paid tier", "intermittent GHA runner flake")

    **If pass rate is 85+/88:**
    Document the results and any remaining failures.

    Target: 85+ of 88 specs passing. Up to 3 specs may remain as known flaky/environmental.
  </action>
  <verify>
    <automated>python3 -c "
import json, os
path = 'test-results/daily-run-validation.json'
assert os.path.exists(path), 'Validation run results file does not exist'
data = json.load(open(path))
pw = data['suites']['playwright']
passed = sum(1 for t in pw['tests'] if t['status'] == 'passed')
total = len(pw['tests'])
print(f'Pass rate: {passed}/{total}')
assert passed >= 85, f'Pass rate {passed}/{total} is below target of 85/88'
print('OK: Pass rate meets target')
"</automated>
  </verify>
  <done>Full daily suite achieves 85+ of 88 specs passing. Results documented in daily-run-validation.json.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of full suite pass rate and regression check</name>
  <files>test-results/daily-run-validation.json</files>
  <action>
    Present the full daily run results to the user for verification. Show pass rate, any remaining failures, and confirm no regressions in the 39 previously passing specs.
  </action>
  <verify>User confirms results are satisfactory</verify>
  <done>User approves the Phase 7 results</done>
  <what-built>Fixed 46 failing Playwright E2E specs across 4 plans: triaged root causes, batch-fixed skill specs (15), signup + auth-dependent specs (~24), and remaining non-auth specs (~7). Full daily suite run completed.</what-built>
  <how-to-verify>
    1. Check the validation results: `python3 -c "import json; d=json.load(open('test-results/daily-run-validation.json')); pw=d['suites']['playwright']; p=sum(1 for t in pw['tests'] if t['status']=='passed'); print(f'{p}/{len(pw[\"tests\"])} specs passing')"`
    2. Verify no previously passing spec is now failing: compare the 39 specs from CONTEXT.md passing list against the validation results
    3. Review any remaining failures -- should be 3 or fewer with documented reasons
    4. If satisfactory, approve to complete Phase 7
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full daily run results exist in test-results/daily-run-validation.json
- Pass rate is 85+ of 88 specs
- All 39 previously passing specs still pass (no regressions)
- Any remaining failures have documented root causes
</verification>

<success_criteria>
- E2E-03: Remaining ~7 non-auth specs are fixed
- E2E-04: Daily pass rate reaches 85+ of 88 specs
- All previously passing specs still pass (zero regressions)
- Human verification approves the final results
</success_criteria>

<output>
After completion, create `.planning/phases/07-e2e-test-suite-repair/07-04-SUMMARY.md`
</output>

---
phase: 07-e2e-test-suite-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test-results/triage-07-01.json
  - .planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03]

must_haves:
  truths:
    - "Actual Playwright error messages are captured for at least 6 representative failing specs"
    - "All 46 failing specs are categorized by root cause (login timing, selector drift, env config, feature drift, external service)"
    - "Each failure category has a documented fix strategy"
  artifacts:
    - path: "test-results/triage-07-01.json"
      provides: "GHA run results with actual Playwright error messages for representative specs"
    - path: ".planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md"
      provides: "Categorized failure list with root cause and fix strategy per category"
  key_links:
    - from: "07-01-TRIAGE.md"
      to: "07-02-PLAN.md, 07-03-PLAN.md, 07-04-PLAN.md"
      via: "Triage categories inform fix strategies in subsequent plans"
      pattern: "Category [A-F]"
---

<objective>
Diagnose the 46 failing Playwright specs by running representative samples on GHA to capture actual error messages, then categorize all failures by root cause.

Purpose: The 03-25 daily run only captured "GitHub Actions conclusion: failure" for all 46 specs -- no Playwright error details. Without actual error messages, fixes would be guesswork. This plan produces the diagnostic data that Plans 02-04 need.

Output: A triage document categorizing all 46 failures by root cause with fix strategies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e2e-test-suite-repair/07-CONTEXT.md
@.planning/phases/07-e2e-test-suite-repair/07-RESEARCH.md
@test-results/daily-run-2026-03-25.json
@test-results/daily-run-2026-03-24.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run 6 representative failing specs on GHA and capture error messages</name>
  <read_first>
    - scripts/run_tests.py (understand how to dispatch specs and capture errors)
    - .github/workflows/playwright-spec.yml (understand GHA workflow inputs and env vars)
    - test-results/daily-run-2026-03-25.json (confirm which specs failed)
    - frontend/apps/web_app/tests/skill-web-search.spec.ts (representative skill spec)
    - frontend/apps/web_app/tests/skill-news-search.spec.ts (second skill spec for comparison)
    - frontend/apps/web_app/tests/signup-flow.spec.ts (representative signup spec)
    - frontend/apps/web_app/tests/status-page.spec.ts (representative non-login spec)
    - frontend/apps/web_app/tests/not-found-404-flow.spec.ts (representative non-login spec)
    - frontend/apps/web_app/tests/api-keys-flow.spec.ts (representative login-dependent spec)
  </read_first>
  <files>test-results/triage-07-01.json</files>
  <action>
    Dispatch 6 representative specs to GHA to capture actual Playwright error messages. Use `gh workflow run` for each:

    1. `skill-web-search.spec.ts` (skill category representative)
    2. `skill-news-search.spec.ts` (second skill for same-vs-different root cause per user decision D-01)
    3. `signup-flow.spec.ts` (signup category)
    4. `api-keys-flow.spec.ts` (login-dependent "other" category)
    5. `status-page.spec.ts` (non-login "other" category)
    6. `not-found-404-flow.spec.ts` (non-login "other" category)

    For each spec:
    ```bash
    gh workflow run playwright-spec.yml \
      --repo glowingkitty/OpenMates \
      --ref dev \
      -f spec={spec-name} \
      -f account=1 \
      -f use_mocks=true
    ```

    Wait for all 6 to complete (poll with `gh run list --workflow=playwright-spec.yml --limit 6`).

    For each completed run, capture the error output:
    ```bash
    gh run view {run_id} --log 2>&1 | grep -A 20 "Error\|FAILED\|expect\|locator\|timeout\|not found"
    ```

    Also download the playwright.json artifact if available:
    ```bash
    gh run download {run_id} -n test-results -D /tmp/triage-{spec}
    ```

    Save all results to `test-results/triage-07-01.json` with structure:
    ```json
    {
      "triage_date": "2026-03-27",
      "specs": {
        "skill-web-search.spec.ts": { "run_id": "...", "status": "...", "error": "..." },
        ...
      }
    }
    ```

    IMPORTANT: Do NOT run specs locally. All specs must run on GHA per user decision.
    IMPORTANT: If `gh workflow run` requires auth, dispatch via the GHA API directly.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('test-results/triage-07-01.json')); assert len(d['specs']) == 6; assert all('error' in v or 'status' in v for v in d['specs'].values()); print('OK: 6 specs triaged')"</automated>
  </verify>
  <done>6 representative specs have been run on GHA with actual Playwright error messages captured in triage-07-01.json</done>
</task>

<task type="auto">
  <name>Task 2: Categorize all 46 failing specs by root cause and document fix strategies</name>
  <read_first>
    - test-results/triage-07-01.json (actual error messages from Task 1)
    - test-results/daily-run-2026-03-24.json (historical error data for cross-reference)
    - test-results/daily-run-2026-03-25.json (current failure list)
    - frontend/apps/web_app/tests/helpers/chat-test-helpers.ts (shared login helper)
    - frontend/apps/web_app/tests/signup-flow-helpers.ts (shared signup helpers)
    - frontend/apps/web_app/tests/helpers/embed-test-helpers.ts (shared embed helpers)
    - frontend/apps/web_app/tests/helpers/env-guard.ts (env var skip guards)
    - frontend/apps/web_app/tests/console-monitor.ts (console error monitor)
  </read_first>
  <files>.planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md</files>
  <action>
    Using the actual error messages from Task 1, categorize ALL 46 failing specs.

    For each of the 46 failing specs, determine the root cause category by:
    1. Checking if the spec's error matches a pattern from the 6 triaged specs
    2. Grepping the spec source for login patterns (inline login vs `loginToTestAccount()`)
    3. Checking the spec's imports and env var dependencies
    4. Checking if the spec was in the 03-24 failure list (it was NOT -- all 46 are new since 03-25)

    The 46 failing specs to categorize:
    **Skill specs (15):** skill-code-docs, skill-events-search, skill-health-appointments, skill-images-search, skill-maps-search, skill-math-calculate, skill-news-search, skill-reminder-set, skill-shopping-search, skill-travel-connections, skill-travel-stays, skill-videos-search, skill-videos-transcript, skill-web-read, skill-web-search
    **Signup specs (4):** signup-flow, signup-flow-passkey, signup-flow-polar, signup-skip-2fa-flow
    **Other specs (27):** a11y-pages, api-keys-flow, background-chat-notification, backup-code-login-flow, backup-codes-settings, cli-file-upload, code-generation-multiturn, connection-resilience, daily-inspiration-chat-flow, default-model-settings, docs-links, embed-showcase, message-sync, model-override, multi-session-encryption, newsletter-flow, not-found-404-flow, pdf-flow, preview-error, recent-chats-dedup, seo-demo-chat, settings-buy-credits-polar, settings-buy-credits-stripe, share-chat-flow, share-embed-flow, shared-chat-open, status-page

    For each spec, grep for its login pattern:
    ```bash
    grep -l "loginToTestAccount\|#login-password-input\|#login-email-input" frontend/apps/web_app/tests/{spec}.spec.ts
    ```

    Create `.planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md` with:

    ```markdown
    # Phase 7 Triage: E2E Failure Categorization

    ## Category A: [name based on actual errors]
    **Root cause:** [from triage data]
    **Fix strategy:** [specific fix]
    **Specs (N):**
    - spec-name.spec.ts — [specific error]
    ...

    ## Category B: ...
    ...

    ## Fix Priority
    1. Category with most specs first (per user decision: batch-fix per category)
    2. ...
    ```

    Per user decision on skill-* specs: If the 2 triaged skill specs (web-search, news-search) show the same error, note "same root cause confirmed -- batch fix all 15". If different, note each spec's individual issue.

    CRITICAL: The categorization must be based on ACTUAL error messages from triage, not assumptions. If triage data is insufficient for some specs, note them as "needs individual triage" with a recommendation to run that spec on GHA.
  </action>
  <verify>
    <automated>grep -c "## Category" .planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md | xargs -I{} test {} -ge 2 && grep -c "spec.ts" .planning/phases/07-e2e-test-suite-repair/07-01-TRIAGE.md | xargs -I{} test {} -ge 46 && echo "OK: Triage has 2+ categories covering 46+ specs"</automated>
  </verify>
  <done>All 46 failing specs are categorized by root cause in 07-01-TRIAGE.md with fix strategies per category. Each category lists its specs and the specific error pattern.</done>
</task>

</tasks>

<verification>
- triage-07-01.json exists with 6 spec entries, each having error details
- 07-01-TRIAGE.md exists with at least 2 failure categories
- Every one of the 46 failing spec names appears in the triage document
- Skill spec same-vs-different root cause determination is documented (per user decision)
- Fix strategies are specific enough for Plans 02-04 to execute without re-diagnosis
</verification>

<success_criteria>
- 6 representative specs have been run on GHA with actual error output captured
- All 46 specs are categorized into failure groups with root cause and fix strategy
- The skill-* spec root cause question (same vs different) is answered with evidence
- Plans 02-04 can proceed with concrete fix strategies, not guesses
</success_criteria>

<output>
After completion, create `.planning/phases/07-e2e-test-suite-repair/07-01-SUMMARY.md`
</output>

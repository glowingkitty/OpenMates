---
phase: 06-opentelemetry-distributed-tracing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/directus/schemas/users.yml
  - backend/shared/python_utils/tracing/user_tier.py
  - frontend/packages/ui/src/i18n/sources/settings/privacy.yml
  - frontend/packages/ui/src/components/settings/SettingsPrivacy.svelte
autonomous: false
requirements: [OTEL-06]

must_haves:
  truths:
    - "Directus users collection has a debug_logging_opted_in boolean field defaulting to false"
    - "Settings UI shows a Debug Logging toggle under Privacy & Security section"
    - "Toggle disclosure text explains what is collected and what is never collected"
    - "Toggling the setting persists to Directus via existing settings save flow"
  artifacts:
    - path: "backend/core/directus/schemas/users.yml"
      provides: "debug_logging_opted_in field definition"
      contains: "debug_logging_opted_in"
    - path: "frontend/packages/ui/src/components/settings/SettingsPrivacy.svelte"
      provides: "Debug Logging toggle UI"
      contains: "debug_logging_opted_in"
  key_links:
    - from: "frontend/packages/ui/src/components/settings/SettingsPrivacy.svelte"
      to: "Directus users collection"
      via: "existing settings save WebSocket flow"
      pattern: "debug_logging_opted_in"
---

<objective>
Add the user opt-in mechanism for detailed debug logging (Tier 3 traces). This includes the Directus schema field, i18n strings, and a Settings UI toggle.

Purpose: Users must explicitly opt in to Tier 3 tracing in production. This plan creates the data model and UI for that consent mechanism.

Output: Directus `debug_logging_opted_in` field on users, Settings toggle with disclosure text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-opentelemetry-distributed-tracing/06-CONTEXT.md

<interfaces>
<!-- Existing patterns for Directus schema and Settings UI -->

From backend/core/directus/schemas/users.yml (field pattern):
```yaml
    field_name:
      type: boolean
      default: false
      note: "Description of what this field does."
```

Settings UI uses canonical components from settings/elements/.
The Privacy settings section already exists in SettingsPrivacy.svelte.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Directus schema field and i18n strings for debug logging opt-in</name>
  <files>
    backend/core/directus/schemas/users.yml,
    frontend/packages/ui/src/i18n/sources/settings/privacy.yml
  </files>
  <read_first>
    backend/core/directus/schemas/users.yml,
    frontend/packages/ui/src/i18n/sources/settings/privacy.yml
  </read_first>
  <action>
    1. In `backend/core/directus/schemas/users.yml`, add a new field at the end of the fields section:
       ```yaml
           debug_logging_opted_in:
             type: boolean
             default: false
             note: "User opted in to detailed debug logging (Tier 3 traces). Controlled via Settings > Privacy & Security."
       ```

    2. In `frontend/packages/ui/src/i18n/sources/settings/privacy.yml`, add keys under the appropriate locale sections. Find the existing structure and add:
       - `debug_logging_title`: "Debug Logging"
       - `debug_logging_description`: "Help improve OpenMates by sharing detailed trace data when errors occur. This includes request timing, service interactions, and error context."
       - `debug_logging_never_collected`: "Your encrypted message content is never collected, regardless of this setting."
       - `debug_logging_toggle_label`: "Enable detailed debug logging"

    3. After editing the YAML source, run `cd frontend/packages/ui && npm run build:translations` to regenerate JSON locale files.
  </action>
  <verify>
    <automated>grep "debug_logging_opted_in" /home/superdev/projects/OpenMates/backend/core/directus/schemas/users.yml && grep "debug_logging" /home/superdev/projects/OpenMates/frontend/packages/ui/src/i18n/sources/settings/privacy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `backend/core/directus/schemas/users.yml` contains `debug_logging_opted_in:`
    - `backend/core/directus/schemas/users.yml` contains `type: boolean` for the new field
    - `backend/core/directus/schemas/users.yml` contains `default: false` for the new field
    - `frontend/packages/ui/src/i18n/sources/settings/privacy.yml` contains `debug_logging_title`
    - `frontend/packages/ui/src/i18n/sources/settings/privacy.yml` contains `debug_logging_never_collected`
  </acceptance_criteria>
  <done>
    Directus schema has debug_logging_opted_in boolean field. i18n YAML sources have all 4 translation keys. JSON translations regenerated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Debug Logging toggle to Settings Privacy UI</name>
  <files>
    frontend/packages/ui/src/components/settings/SettingsPrivacy.svelte
  </files>
  <read_first>
    frontend/packages/ui/src/components/settings/SettingsPrivacy.svelte,
    frontend/packages/ui/src/components/settings/elements/
  </read_first>
  <action>
    1. Read `SettingsPrivacy.svelte` to understand the existing component structure and how it saves settings.

    2. Add a new section for Debug Logging using the canonical settings elements pattern. The section should include:
       - A section header using the existing settings section component pattern (e.g., `SettingsSection` or equivalent)
       - A toggle element (use the existing toggle/switch settings element from `settings/elements/`)
       - Bound to a `debug_logging_opted_in` field in the user settings object
       - Disclosure text below the toggle explaining what is collected and what is never collected
       - Use i18n keys: `$t('settings.privacy.debug_logging_title')`, `$t('settings.privacy.debug_logging_description')`, `$t('settings.privacy.debug_logging_never_collected')`, `$t('settings.privacy.debug_logging_toggle_label')`

    3. The toggle must use the same save mechanism as other privacy settings in this component (likely WebSocket message or API call — follow existing pattern).

    4. Place the Debug Logging section AFTER all existing privacy sections (it's a new, optional feature — not core privacy).

    5. Add a small note/disclaimer styled with `var(--color-font-secondary)` and `var(--font-size-p)` explaining: encrypted message content is never collected.
  </action>
  <verify>
    <automated>grep -c "debug_logging" /home/superdev/projects/OpenMates/frontend/packages/ui/src/components/settings/SettingsPrivacy.svelte</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsPrivacy.svelte` contains `debug_logging_opted_in`
    - `SettingsPrivacy.svelte` contains at least one `$t('settings.privacy.debug_logging` i18n reference
    - `SettingsPrivacy.svelte` contains `debug_logging_never_collected`
    - grep returns count >= 3 (toggle binding, title, description references)
  </acceptance_criteria>
  <done>
    Settings > Privacy & Security shows a Debug Logging toggle with disclosure text. Toggle persists to user settings. Encrypted content disclaimer is visible.
  </done>
</task>

<task type="checkpoint:human-verify" gate="soft">
  <name>Task 3: Verify Debug Logging toggle in browser</name>
  <files>frontend/packages/ui/src/components/settings/SettingsPrivacy.svelte</files>
  <action>
    Visual verification of the Debug Logging toggle in the Settings UI. No code changes -- this is a human verification checkpoint.
  </action>
  <verify>
    <automated>grep -c "debug_logging" /home/superdev/projects/OpenMates/frontend/packages/ui/src/components/settings/SettingsPrivacy.svelte</automated>
  </verify>
  <done>User confirms toggle is visible, functional, and persists across page reloads.</done>
  <what-built>Debug Logging opt-in toggle in Settings > Privacy and Security, backed by Directus schema field</what-built>
  <how-to-verify>
    1. Apply Directus schema: restart cms-setup container or apply manually
    2. Open the app, go to Settings > Privacy and Security
    3. Scroll to Debug Logging section
    4. Verify toggle is present with disclosure text
    5. Toggle on, reload page, verify it persists
    6. Toggle off, reload, verify it persists
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `grep "debug_logging_opted_in" backend/core/directus/schemas/users.yml` -- field exists
2. `grep -c "debug_logging" frontend/packages/ui/src/components/settings/SettingsPrivacy.svelte` -- returns >= 3
3. i18n keys present in privacy.yml source
</verification>

<success_criteria>
- Directus users collection schema includes `debug_logging_opted_in` boolean (default false)
- Settings UI shows Debug Logging toggle with disclosure text
- Toggle uses existing settings save pattern (no new API endpoints)
- i18n strings cover title, description, disclaimer, and toggle label
</success_criteria>

<output>
After completion, create `.planning/phases/06-opentelemetry-distributed-tracing/06-02-SUMMARY.md`
</output>

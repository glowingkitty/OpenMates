---
phase: 03-key-management-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/packages/ui/src/services/encryption/ChatKeyManager.ts
  - frontend/packages/ui/src/services/encryption/__tests__/ChatKeyManager.test.ts
  - frontend/packages/ui/src/services/hiddenChatService.ts
  - docs/architecture/core/master-key-cross-device.md
autonomous: true
requirements: [KEYS-06, KEYS-03]

must_haves:
  truths:
    - "BroadcastChannel keyLoaded messages warm the receiving tab's key cache for chats with pending operations"
    - "After key load or creation, keyLoaded is broadcast to all other tabs"
    - "Hidden chat key wrapping is routed through ChatKeyManager methods instead of direct crypto imports"
    - "Master key cross-device mechanism is formally documented with validation steps"
  artifacts:
    - path: "frontend/packages/ui/src/services/encryption/ChatKeyManager.ts"
      provides: "BroadcastChannel keyLoaded handler, broadcastKeyLoaded helper, rewrapKey method for hidden chats"
      contains: "keyLoaded"
    - path: "docs/architecture/core/master-key-cross-device.md"
      provides: "Formal documentation of cross-device key distribution mechanism"
      contains: "PBKDF2"
  key_links:
    - from: "ChatKeyManager.handleCrossTabMessage"
      to: "receiveKeyFromServer"
      via: "keyLoaded message triggers cache warm"
      pattern: "keyLoaded.*receiveKeyFromServer"
    - from: "ChatKeyManager.broadcastKeyLoaded"
      to: "BroadcastChannel.postMessage"
      via: "broadcast after key load/create"
      pattern: "broadcastKeyLoaded"
    - from: "hiddenChatService"
      to: "ChatKeyManager.rewrapKey"
      via: "hide/unhide key wrapping"
      pattern: "rewrapKey"
---

<objective>
Complete BroadcastChannel key propagation, consolidate hidden chat key wrapping into ChatKeyManager, and formalize the cross-device master key mechanism.

Purpose: Eliminates the last bypass gap (hidden chat wrapping per D-01) and ensures cross-tab key availability without redundant IDB reads. Formalizes the master key cross-device design that Phase 1 confirmed architecturally sound (KEYS-06).
Output: Working BroadcastChannel keyLoaded path, ChatKeyManager rewrapKey method, cross-device architecture doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-key-management-hardening/03-CONTEXT.md
@.planning/phases/03-key-management-hardening/03-RESEARCH.md
@docs/architecture/core/master-key-lifecycle.md

@frontend/packages/ui/src/services/encryption/ChatKeyManager.ts
@frontend/packages/ui/src/services/encryption/__tests__/ChatKeyManager.test.ts
@frontend/packages/ui/src/services/hiddenChatService.ts

<interfaces>
From ChatKeyManager.ts:
```typescript
type CrossTabMessage =
  | { type: "clearAll" }
  | { type: "keyLoaded"; chatId: string; encryptedChatKey: string };

// handleCrossTabMessage at line 232 -- keyLoaded branch is currently a no-op comment
// broadcastChannel field at line 201-209 -- BroadcastChannel instance, nullable
// pendingOps at line 177 -- Map<string, QueuedOperation[]>
// receiveKeyFromServer(chatId, encryptedChatKey) at line 496 -- unwraps and injects key
```

From hiddenChatService.ts:
```typescript
// line 15: import { getKeyFromStorage, getEmailSalt } from "./cryptoService";
// line 17-18: import { encryptChatKeyWithMasterKey, decryptChatKeyWithMasterKey } from ...
// line 155: getKeyFromStorage() for PBKDF2 derivation of combined secret
// line 257: decryptChatKeyWithMasterKey for unhide (unwrap from master key)
// line 490: decryptChatKeyWithMasterKey to test key type
// line 623: encryptChatKeyWithMasterKey for unhide (rewrap with master key)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete BroadcastChannel keyLoaded handler and broadcast helper</name>
  <files>
    frontend/packages/ui/src/services/encryption/ChatKeyManager.ts
    frontend/packages/ui/src/services/encryption/__tests__/ChatKeyManager.test.ts
  </files>
  <read_first>
    - frontend/packages/ui/src/services/encryption/ChatKeyManager.ts
    - frontend/packages/ui/src/services/encryption/__tests__/ChatKeyManager.test.ts
  </read_first>
  <behavior>
    - Test: keyLoaded message for a chat with pending ops triggers receiveKeyFromServer and flushes the queue
    - Test: keyLoaded message for a chat with NO pending ops is ignored (no unnecessary async work -- Pitfall 4)
    - Test: keyLoaded message for a chat where key already exists in memory is ignored
    - Test: After createKeyForNewChat, broadcastKeyLoaded is called with chatId and encryptedChatKey
    - Test: After receiveKeyFromServer succeeds, broadcastKeyLoaded is called
    - Test: broadcastKeyLoaded is a no-op when broadcastChannel is null (SSR)
  </behavior>
  <action>
    **Tests first (RED):**
    Add a BroadcastChannel mock to the test file. Since BroadcastChannel in jsdom may not exist, mock it:
    ```typescript
    const broadcastMessages: CrossTabMessage[] = [];
    vi.stubGlobal("BroadcastChannel", class MockBroadcastChannel {
      onmessage: ((event: MessageEvent) => void) | null = null;
      postMessage(data: any) { broadcastMessages.push(data); }
      close() {}
    });
    ```
    Clear `broadcastMessages` in beforeEach.

    Write test describe "ChatKeyManager -- BroadcastChannel keyLoaded (KEYS-06 cross-tab)" with the 6 tests above.

    **Implementation (GREEN):**

    1. Add `private broadcastKeyLoaded(chatId: string, encryptedChatKey: string): void` method:
       - If `this.broadcastChannel` is null, return (SSR guard)
       - Post `{ type: "keyLoaded", chatId, encryptedChatKey } satisfies CrossTabMessage`

    2. Update `handleCrossTabMessage` keyLoaded branch (replace the no-op comment at line 241-243):
       - If `this.keys.has(msg.chatId)`, return (already have this key)
       - If `!this.pendingOps.has(msg.chatId) || this.pendingOps.get(msg.chatId)!.length === 0`, return (no pending work, lazy-load when needed -- Pitfall 4 prevention per RESEARCH.md)
       - Otherwise: call `this.receiveKeyFromServer(msg.chatId, msg.encryptedChatKey).catch(...)` to warm cache and flush pending ops

    3. Add `broadcastKeyLoaded` call sites:
       - In `createAndPersistKey`, after successful persist, call `this.broadcastKeyLoaded(chatId, encryptedChatKey)`
       - In `receiveKeyFromServer`, after successful injection, call `this.broadcastKeyLoaded(chatId, encryptedChatKey)` -- but only if the key was NOT received via cross-tab broadcast (to prevent infinite loops). Add a `private _receivingFromBroadcast = false` flag, set it true during handleCrossTabMessage keyLoaded processing, check it in receiveKeyFromServer before broadcasting.

    Per D-02: This only affects chat keys via ChatKeyManager. Master key operations are untouched.
  </action>
  <verify>
    <automated>cd frontend/packages/ui && npx vitest run src/services/encryption/__tests__/ChatKeyManager.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "keyLoaded" frontend/packages/ui/src/services/encryption/ChatKeyManager.ts shows handler implementation (not just the comment)
    - grep "broadcastKeyLoaded" frontend/packages/ui/src/services/encryption/ChatKeyManager.ts returns >= 3 matches (definition + 2 call sites)
    - grep "pendingOps.has\|pendingOps.get" frontend/packages/ui/src/services/encryption/ChatKeyManager.ts appears in keyLoaded handler (Pitfall 4 guard)
    - grep "_receivingFromBroadcast" frontend/packages/ui/src/services/encryption/ChatKeyManager.ts returns matches (loop prevention)
    - All tests pass: cd frontend/packages/ui && npx vitest run src/services/encryption/__tests__/ (exit code 0)
  </acceptance_criteria>
  <done>BroadcastChannel keyLoaded handler processes messages for chats with pending ops, ignores others. broadcastKeyLoaded fires after key creation and server receive. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add rewrapKey to ChatKeyManager and update hiddenChatService bypass + formalize cross-device doc</name>
  <files>
    frontend/packages/ui/src/services/encryption/ChatKeyManager.ts
    frontend/packages/ui/src/services/hiddenChatService.ts
    docs/architecture/core/master-key-cross-device.md
    frontend/packages/ui/src/services/encryption/__tests__/ChatKeyManager.test.ts
  </files>
  <read_first>
    - frontend/packages/ui/src/services/encryption/ChatKeyManager.ts
    - frontend/packages/ui/src/services/hiddenChatService.ts
    - docs/architecture/core/master-key-lifecycle.md
  </read_first>
  <action>
    **Part A: Add rewrapKey method to ChatKeyManager (KEYS-03 bypass closure per D-01)**

    Add `async rewrapKey(chatId: string, decryptWithOldKey: (encrypted: string) => Promise<Uint8Array | null>, encryptWithNewKey: (key: Uint8Array) => Promise<string>): Promise<string | null>` method to ChatKeyManager:

    1. Get current key from `this.keys.get(chatId)` -- if not in memory, return null (caller must ensure key is loaded)
    2. Encrypt with new wrapping key: `const newEncrypted = await encryptWithNewKey(currentKey)`
    3. Update provenance: source stays the same, timestamp updates
    4. Return `newEncrypted`

    This lets hiddenChatService call `chatKeyManager.rewrapKey(chatId, decryptFn, encryptFn)` instead of directly importing `encryptChatKeyWithMasterKey`/`decryptChatKeyWithMasterKey`. ChatKeyManager stays unaware of hidden chat specifics -- it just re-wraps using the provided functions.

    **Part B: Update hiddenChatService to use ChatKeyManager.rewrapKey**

    In `hiddenChatService.ts`, for the hide/unhide flows at lines 257, 490, 623:
    - Replace direct `decryptChatKeyWithMasterKey`/`encryptChatKeyWithMasterKey` calls with calls through `chatKeyManager.rewrapKey()` where the key is being re-wrapped (unhide flow at line 623)
    - For line 257 (unhide -- decrypt from master key to get raw key): this is actually a `receiveKeyFromServer` pattern. Use `chatKeyManager.receiveKeyFromServer(chatId, encryptedChatKey)` instead of direct unwrap
    - For line 490 (test if key is master-key-encrypted): this is a diagnostic check. Add a comment: `// Legitimate bypass: testing wrapping type, not performing crypto operation`
    - For line 155 (`getKeyFromStorage` for PBKDF2): add comment: `// Legitimate bypass per D-01: master key operation for combined secret derivation`
    - Keep `encryptChatKeyWithMasterKey`/`decryptChatKeyWithMasterKey` imports for cases where they are genuinely needed (the combined secret wrapping), but route key re-wrapping through ChatKeyManager where possible

    Note: The research (Open Question 3) warned about circular dependency. Avoid it: hiddenChatService imports from chatKeyManager (already does for `setHiddenChatKeyDecryptor`). ChatKeyManager does NOT import from hiddenChatService.

    **Part C: Formalize cross-device master key documentation (KEYS-06)**

    Create `docs/architecture/core/master-key-cross-device.md` documenting:
    1. How the master key is derived (PBKDF2 from user credentials -- deterministic, same on every device)
    2. Why cross-device "distribution" is not needed (each device derives the same master key independently)
    3. The login flow on a new device: authenticate -> derive master key -> bulk load encrypted_chat_keys from server -> unwrap each with master key -> keys are available
    4. Validation: on login, verify master key can unwrap at least one known encrypted_chat_key (fail fast if credentials changed)
    5. Reference the Phase 1 master-key-lifecycle.md for the full derivation chain
    6. State explicitly: "No key transport protocol needed -- deterministic derivation is the distribution mechanism"

    This formalizes what Phase 1 confirmed. Per KEYS-06: "formally designed and implemented" means documented with the validation step.

    Add a test for rewrapKey:
    - Test: rewrapKey with key in memory returns re-wrapped form
    - Test: rewrapKey with no key in memory returns null
  </action>
  <verify>
    <automated>cd frontend/packages/ui && npx vitest run src/services/encryption/__tests__/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "rewrapKey" frontend/packages/ui/src/services/encryption/ChatKeyManager.ts returns matches
    - grep "Legitimate bypass" frontend/packages/ui/src/services/hiddenChatService.ts returns >= 1 match
    - test -f docs/architecture/core/master-key-cross-device.md (file exists)
    - grep "deterministic derivation" docs/architecture/core/master-key-cross-device.md returns match
    - grep "PBKDF2" docs/architecture/core/master-key-cross-device.md returns match
    - All encryption tests pass: cd frontend/packages/ui && npx vitest run src/services/encryption/__tests__/ (exit code 0)
  </acceptance_criteria>
  <done>Hidden chat bypass resolved with rewrapKey method and documented bypass comments. Cross-device mechanism formally documented. All tests green.</done>
</task>

</tasks>

<verification>
1. `cd frontend/packages/ui && npx vitest run src/services/encryption/__tests__/` -- full suite passes
2. `grep "keyLoaded" frontend/packages/ui/src/services/encryption/ChatKeyManager.ts | grep -v "comment\|no-op"` -- handler is real
3. `test -f docs/architecture/core/master-key-cross-device.md` -- doc exists
4. `grep -c "Legitimate bypass" frontend/packages/ui/src/services/hiddenChatService.ts` -- bypass items documented
</verification>

<success_criteria>
- BroadcastChannel keyLoaded handler warms cache for chats with pending ops (KEYS-06 cross-tab)
- keyLoaded messages for idle chats are ignored (Pitfall 4 prevention)
- Hidden chat key wrapping routed through ChatKeyManager where possible (KEYS-03)
- Remaining legitimate bypasses documented with comments (D-01)
- Cross-device master key mechanism formally documented (KEYS-06)
- All 65+ encryption tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-key-management-hardening/03-02-SUMMARY.md`
</output>

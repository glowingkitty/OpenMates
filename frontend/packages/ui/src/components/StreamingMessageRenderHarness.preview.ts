// frontend/packages/ui/src/components/StreamingMessageRenderHarness.preview.ts
// Preview metadata for the deterministic deployed streaming-render harness.
// The harness owns its cumulative message snapshots and requires no props.
// It is intended for automated performance-contract verification only.
// Spec: docs/specs/streaming-message-render-convergence/spec.yml

export default {
  title: 'Streaming message render harness',
  description: 'Exercises canonical assistant streaming compilation and rendering.',
};

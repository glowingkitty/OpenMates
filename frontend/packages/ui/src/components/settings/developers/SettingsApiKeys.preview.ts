/**
 * Deterministic preview fixture for API-key scope selection settings.
 * Opens the create screen without account state or network dependencies.
 * Access path: /dev/preview/settings/developers/SettingsApiKeys?chrome=0.
 * Product Plan: docs/plans/api-key-scopes-v1/plan.yml.
 */

export default {
  activeSettingsView: 'developers/api-keys/create',
};

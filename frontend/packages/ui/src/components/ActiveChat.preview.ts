/**
 * Default workspace shell for isolated pane-transition verification.
 * The focused browser spec supplies fictional embed events through the same
 * document event boundary used by message previews. No account or provider
 * request is needed, and missing child IDs exercise the real loading shell.
 * See docs/architecture/frontend/embed-workspace-transitions.md.
 */
export default {};

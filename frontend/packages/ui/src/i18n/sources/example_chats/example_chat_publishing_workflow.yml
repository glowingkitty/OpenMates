title:
  context: "Title of example chat: Example chat publishing workflow"
  en: "Example chat publishing workflow"
  de: "Example chat publishing workflow"
  zh: "Example chat publishing workflow"
  es: "Example chat publishing workflow"
  fr: "Example chat publishing workflow"
  pt: "Example chat publishing workflow"
  ru: "Example chat publishing workflow"
  ja: "Example chat publishing workflow"
  ko: "Example chat publishing workflow"
  it: "Example chat publishing workflow"
  tr: "Example chat publishing workflow"
  vi: "Example chat publishing workflow"
  id: "Example chat publishing workflow"
  pl: "Example chat publishing workflow"
  nl: "Example chat publishing workflow"
  ar: "Example chat publishing workflow"
  hi: "Example chat publishing workflow"
  th: "Example chat publishing workflow"
  cs: "Example chat publishing workflow"
  sv: "Example chat publishing workflow"
  he: "Example chat publishing workflow"
  verified_by_human: []

summary:
  context: "Summary of example chat: Example chat publishing workflow"
  en: "Create a reusable manual workflow for publishing public OpenMates example chats."
  de: "Create a reusable manual workflow for publishing public OpenMates example chats."
  zh: "Create a reusable manual workflow for publishing public OpenMates example chats."
  es: "Create a reusable manual workflow for publishing public OpenMates example chats."
  fr: "Create a reusable manual workflow for publishing public OpenMates example chats."
  pt: "Create a reusable manual workflow for publishing public OpenMates example chats."
  ru: "Create a reusable manual workflow for publishing public OpenMates example chats."
  ja: "Create a reusable manual workflow for publishing public OpenMates example chats."
  ko: "Create a reusable manual workflow for publishing public OpenMates example chats."
  it: "Create a reusable manual workflow for publishing public OpenMates example chats."
  tr: "Create a reusable manual workflow for publishing public OpenMates example chats."
  vi: "Create a reusable manual workflow for publishing public OpenMates example chats."
  id: "Create a reusable manual workflow for publishing public OpenMates example chats."
  pl: "Create a reusable manual workflow for publishing public OpenMates example chats."
  nl: "Create a reusable manual workflow for publishing public OpenMates example chats."
  ar: "Create a reusable manual workflow for publishing public OpenMates example chats."
  hi: "Create a reusable manual workflow for publishing public OpenMates example chats."
  th: "Create a reusable manual workflow for publishing public OpenMates example chats."
  cs: "Create a reusable manual workflow for publishing public OpenMates example chats."
  sv: "Create a reusable manual workflow for publishing public OpenMates example chats."
  he: "Create a reusable manual workflow for publishing public OpenMates example chats."
  verified_by_human: []

message_1:
  context: "user message 1 in example chat: Example chat publishing workflow. Keep markdown, JSON code blocks, embed links, source quote links, and placeholders unchanged."
  en: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  de: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  zh: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  es: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  fr: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  pt: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  ru: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  ja: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  ko: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  it: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  tr: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  vi: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  id: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  pl: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  nl: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  ar: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  hi: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  th: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  cs: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  sv: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  he: "@skill:workflows:create-or-modify Create exactly one disabled manual workflow titled Example chat publishing checklist. It should have a manual trigger and three manual steps: verify the real CLI source chats, scaffold the example chat files, and run the deployed example-chat Playwright check. After the workflow tool call, reply with exactly one sentence: Created the workflow. Do not include markdown links, embed links, bullets, next steps, reminders, schedules, or extra embeds."
  verified_by_human: []

message_2:
  context: "assistant message 2 in example chat: Example chat publishing workflow. Keep markdown, JSON code blocks, embed links, source quote links, and placeholders unchanged."
  en: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  de: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  zh: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  es: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  fr: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  pt: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  ru: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  ja: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  ko: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  it: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  tr: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  vi: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  id: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  pl: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  nl: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  ar: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  hi: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  th: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  cs: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  sv: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  he: |
    ```json
    {"type": "app_skill_use", "embed_id": "53d32845-0b88-47c0-ace0-db90ae95d89b", "app_id": "workflows", "skill_id": "create-or-modify"}
    ```
    
    Created the workflow.
  verified_by_human: []


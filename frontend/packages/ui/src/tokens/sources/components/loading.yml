# Loading state primitives (loading-text label, skeleton-line placeholder).
# Consumed by build-tokens.js → emits .ds-* CSS classes + DS.* Swift structs.
# Populated incrementally per E-α.5 in plan cheeky-snuggling-zebra.md.

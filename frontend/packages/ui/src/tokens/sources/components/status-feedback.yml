# Status feedback primitives (error/success/warning icons + titles).
# Consumed by build-tokens.js → emits .ds-* CSS classes + DS.* Swift structs.
# Populated incrementally per E-α.4 in plan cheeky-snuggling-zebra.md.

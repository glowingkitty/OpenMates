/**
 * Component preview index — client-side only.
 * No SSR needed for dev tooling, and import.meta.glob
 * for the UI package components must run in the browser context.
 */
export const ssr = false;
export const prerender = false;

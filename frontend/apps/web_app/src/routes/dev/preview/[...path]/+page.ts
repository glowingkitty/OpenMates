/**
 * Dynamic component preview — client-side only.
 * No SSR needed for dev tooling, and import.meta.glob / dynamic
 * component loading must run in the browser context.
 */
export const ssr = false;
export const prerender = false;

name: Publish GitHub Release

on:
  workflow_run:
    workflows:
      - Publish CLI
      - Publish Python SDK
      - Publish Self-Host Images
    types: [completed]
  workflow_dispatch:
    inputs:
      commit:
        description: Exact main commit whose artifacts should be verified and released
        required: true
        type: string

permissions:
  actions: read
  contents: write
  packages: read

concurrency:
  group: publish-github-release-${{ github.event.workflow_run.head_sha || inputs.commit }}
  cancel-in-progress: false

jobs:
  publish:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.event == 'push' &&
       github.event.workflow_run.head_branch == 'main' &&
       github.event.workflow_run.conclusion == 'success')
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      GH_TOKEN: ${{ github.token }}
      TARGET_SHA: ${{ github.event.workflow_run.head_sha || inputs.commit }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_SHA }}
          fetch-depth: 0
          fetch-tags: true

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Verify artifacts and publish release
        run: >-
          python3 scripts/publish_stable_github_release.py
          --repo "${GITHUB_REPOSITORY}"
          --commit "${TARGET_SHA}"
          --apply
